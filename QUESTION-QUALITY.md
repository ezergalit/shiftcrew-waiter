# רשימת בקרת איכות לשאלות — Menu Trainer

> **חובה לקרוא לפני שינוי ב-`src/lib/questionEngine.js` או בכל מצב משחק.**
> כל טעות שנתפסה פעם אחת נרשמת כאן עם הכלל שמונע אותה, ועם בדיקה אוטומטית
> ב-`tests/engine.test.mjs` שנכשלת אם היא חוזרת. **אל תמחק שורות מכאן** — הרשימה
> היא הזיכרון של הפרויקט, לא TODO.
>
> הרץ לפני כל commit: `node tests/engine.test.mjs && node tests/path.test.mjs`

## העיקרון

שאלה טובה נכשלת אצל מי שלא מכיר את התפריט **הזה**, ומצליחה אצל מי שכן. שאלה
שאפשר לענות עליה עם ידע כללי, היגיון לשוני, או תבנית ויזואלית — היא באג, גם אם
התשובה "נכונה".

## טעויות שנתפסו (ולא יחזרו)

| # | הטעות | דוגמה אמיתית | הכלל שמונע אותה | בדיקה |
|---|---|---|---|---|
| 1 | **דליפת שם** — התיאור הנכון מכיל את שם המנה | "סלמון אבוקדו" ⇒ "סלמון ואבוקדו…" | `maskNameLeak` ממסך כל מילה מהשם (כולל תחילית עברית) בכל האופציות באופן אחיד | `leak` |
| 2 | **מסיחים אקראיים** — פתיר באלימינציה בלי ידע | שאלה על פסטה עם "קינוח" כמסיח | `pickDistractors` ממיין לפי דמיון (מרכיבים ×2 + מילות תיאור), אותה קטגוריה קודם | `distractor-similarity` |
| 3 | **שאלת ידע כללי** — נענית בלי התפריט | "בס — דג / בשר / ירק?" | `qServingStyle` רק כשתוויות הקטגוריה **מבניות** (יש להן זנב תיאורי אחרי מקף) ולא שמות מנה גנריים; + שער `hasGeneralKnowledgeGiveaway` | `general-knowledge` |
| 4 | **חפיפת מילים חד-משמעית** — רק אופציה אחת חולקת מילה עם הנושא | נושא "מאקי סלמון", אופציות: "…סלמון…", "…טונה…", "…בס…" | `singleOverlapGiveaway` — אם בדיוק אופציה אחת חולקת מילת תוכן עם הנושא, השאלה נדחית | `single-overlap` |
| 5 | **התשובה הנכונה ארוכה בהרבה** — תבנית ויזואלית מוכרת ממבחנים | נכונה 90 תווים, מסיחים 20 | `lengthOutlier` — נכונה לא תהיה ארוכה/קצרה פי >2 מחציון האחרות | `length-outlier` |
| 6 | **אופציות כפולות למעשה** — שתי אופציות זהות אחרי נרמול | "בס" (סשימי) ו-"בס" (ניגירי) התמזגו ל-Set והשאלה ירדה בשקט | `displayName` מסווג לפי סגנון הגשה; `nearDuplicateOptions` דוחה חפיפה ≥85% | `near-duplicate` |
| 7 | **מיסוך כתמרור** — רק לאופציה אחת יש ▢▢▢ | qChanges: הנכונה ממוסכת, המסיחים לא | דירוג מסיחים לפי "מסיכתיות" תואמת לנכונה | `mask-tell` |
| 8 | **שאלה על שדה שהבעלים לא בחר לבחון** | מבחן מחיר במסעדה שלא אכפת לה ממחירים | ה-deck נבנה לפי `facets` מ-`exam_config` (הבעלים מדרג), לא רשימה קבועה | `facet-respect` |
| 9 | **שאלה על חלק בתפריט שהמלצר לא פתח** | משחק "מהירות" שואל על קינוחים לפני שנפתחו | כל משחק מקבל `pool` מסונן לקטגוריות פתוחות בלבד (`learningPath.js`) | `scope-respect` |
| 10 | **תיאור סתום שהועתק כלשונו** | "ירק לבחירה — עד 3 מרכיבים" | כלל 2 ב-prompt של `menu-ai-parse` (v7): לנסח מחדש לבהירות, ולשאול את הבעלים כשלא ברור | ידני (Edge Function) |
| 11 | **אלרגן שהומצא** | רול צמחוני סומן "אגוזים" | כלל 4 ב-prompt (v6) + `sanitizeCategories` בשרת | ידני (Edge Function) |
| 12 | **דליפה בכתיב שונה** — אותה מילה, איות אחר, אז השוואת מחרוזות פספסה | תיאור "…ומניפת **לימון** מעל" מול האופציה "**למון** טוויסט" — נמצא במבחן חי, לא בבדיקה | `hebKey` מנרמל ו'/י' (mater lectionis) לפני כל השוואה — במיסוך וגם בשער החפיפה. **לא** מנרמל ה' (חלב/חלבה הן מילים שונות) | `spelling-variant` |
| 13 | **אלרגן חסר בתיוג ⇒ שאלה בלתי-פתירה** — מסיח שהמלצר *רואה* שיש בו את האלרגן, רק שהתיוג שכח אותו | "אורח מבקש מנה ללא **דגים** — איזו אסורה?" והאופציות: מאקי סלמון חם, מאקי סלמון אבוקדו, שרימפס טמפורה. `מאקי סלמון חם` מכיל סלמון אבל תויג `[גלוטן]` בלבד — נמצא במבחן חי | `ingredientsImplying` לומד **מהתפריט עצמו** אילו מרכיבים גוררים אלרגן (מרכיב שרוב המנות שמכילות אותו מתויגות ב-A ⇒ הוא גורר A), ומסיח שנראה לא-נקי נפסל. **בלי ידע מוטמע על אוכל** — נגזר לכל מסעדה מהנתונים שלה. משמש רק כדי *לפסול* מסיח, לעולם לא כדי להוסיף אלרגן למנה | `MIS-TAGGED-ALLERGEN` |
| 14 | **שאלה מיותרת על מנה חד-מרכיבית** — "מה יש בסשימי בס?" התשובה בשם | מבחן סשימי: כל 4 השאלות היו על מרכיבים, וכל תשובה הייתה שם המנה | `qServingCount` — מה שכן אפשר לבחון שם הוא כמות ההגשה שמופיעה בשורת הקטגוריה ("דק (5 פרוסות) / עבה (3 פרוסות)"). ה-facet `serving` זמין עכשיו גם לקטגוריה **בודדת** שנושאת ספירה, לא רק כשיש 3+ קטגוריות להשוות | `COUNT` |
| 15 | **מוקש שסומן כאלרגן** — "דגים" סומן כאלרגן בכ-75% ממנות תפריט סושי | תפריט שכמעט כולו דג: הסימון לא מוסר מידע, וה-25% שלא סומנו נקראים כ"בטוחות". במקביל, סועדת בהריון שלא יכולה לאכול **דג נא** לא קיבלה שום סימון — זו שאלה אחרת לגמרי מאלרגיה | הופרדו שני שדות: `allergens` (בטיחות, 9 ערכים סגורים) ו-`pitfalls` (העדפות: כוסברה, חריף, **דג נא**, שום…). `qPitfallDish` בונה שאלות על מוקשים, ומחזיר `null` כשרוב התפריט נושא את אותו מוקש — בדיוק המצב שהפך את שאלת הדגים ללא-פתירה. ה-Edge Function (v11) מסמן דג נא רק כשהדג מוגש נא, ולא מסמן "דגים" בתפריט שכולו דג | `MIS-TAGGED-ALLERGEN` |
| 16 | **יותר מתשובה נכונה אחת** — "איזה מרכיב לא נמצא במאקי סלמון חם?" עם `טמפורה` **וגם** `שבבי טמפורה` באפשרויות | השערים הקיימים מודדים כמה אפשרויות **נראות** דומות (Jaccard על מילות תוכן) — הם לא יכולים לראות שאפשרות שנייה פשוט **נכונה גם היא**. שני מקרים אמיתיים בתפריט החי: (1) מרכיב שחופף למרכיב אמיתי במנה הוצע כ"לא נמצא"; (2) שני תיאורים שנבדלים במילה אחת קיבלו Jaccard 0.833 — **מתחת** לסף 0.85 — ולכן שניהם הוצגו | `qNotIngredient` פוסל מסיח שמילותיו חופפות מרכיב אמיתי (`hebKey`). `nearDuplicateOptions` פוסל גם הפרש של ≤1 מילת תוכן וגם תת-קבוצה ממש. הבדיקה **גוזרת את האמת מחדש** ולא סומכת על ה-builder: 48 כשלים לפני התיקון, 0 אחריו | `AMBIGUOUS` |

| 17 | **שלילה כפולה בניסוח** — "אורח מבקש מנה **ללא** סויה. איזו מנה **אסורה** לו?" | המשתמש: השאלה בודקת הבנת ניסוח, לא ידע בתפריט — שתי שלילות באותה שאלה מבלבלות | הניסוח הוחלף לחיוב ישיר: `איזו מנה מכילה X?` (אלרגנים) / `באיזו מנה יש X?` (מוקשים). הכלל: **לכל היותר שלילה אחת בשאלה**. הבדיקה סופרת לא/ללא/בלי/אסור בכל prompt ונכשלת על 2+ | `phrasing` |

## שאלות שירות — `serviceBank.js` / `serviceStandard.js` / `serviceExam.js` (2026-08-24)

שאלת תפריט ושאלת שירות נכשלות אחרת. לשאלת תפריט יש **סמכות אחת** (התפריט) ו"ידע כללי"
הוא הכשל העיקרי. לשאלת שירות אין סמכות אוטומטית, והכשל העיקרי הוא **שהמסיחים טיפשים** —
"אורח מתלונן: להקשיב / להתווכח / להתעלם / לצחוק" מודדת אם אתה בן אדם הגון, לא אם הוכשרת.

```bash
node tests/service.test.mjs
```

| # | הטעות | דוגמה אמיתית | הכלל שמונע אותה | בדיקה |
|---|---|---|---|---|
| 18 | **מסיח שאפשר לפסול בלי ידע** | כל מסיח שאינו פעולה מקצועית אמיתית | הבנק בנוי מ**מאגר פעולות**, וכל מסיח חייב להיות **התשובה הנכונה של שאלה אחרת בבנק**. פעולה שלעולם אינה נכונה בשום מקום היא איש קש או חור בכיסוי — ושניהם נראים אותו דבר מבחוץ. הסבב הראשון החזיר **60 כשלים**, והתיקון היה להגדיל את הבנק מ-36 ל-72 שאלות | `ELIMINABLE` |
| 19 | **שתי אופציות שמשמעותן זהה** | שאלת אלרגיה הציעה גם "להביא מנה חדשה במקום זו שיצאה" וגם "להביא מנה חדשה במקום להוריד רכיב" | Jaccard מודד כמה אופציות **נראות** דומות ולא מה הן **אומרות** — בדיוק המגבלה של שורה 16, מכיוון אחר. `CONFLICTS` הוא רשימה מוצהרת ידנית של זוגות שמלצר קורא כאותה תשובה. ⚠️ **נתפס בעין, לא ע"י שער** — ואז הפך לשער | `SEMANTIC-PAIR` |
| 19א | **שער שהיה מחמיר מדי** | אותו שער פסל גם שני מסיחים סותרים, וגם "מה הדבר הראשון?" שמפריד בין שני שלבים עוקבים | זוג מטעה **רק כשאחד מהם הוא התשובה**; שניהם שגויים = אף אחד לא מתלבט. וזוג `sequence` מותר ב-`first`/`order` — זו כל מטרת הניסוח הזה. **שער חדש צריך בדיקה שמוכיחה שהוא מעביר את המקרה התקין** (טעות J) | `SEMANTIC-PAIR` |
| 20 | **הפרומפט נוקב בתשובה שלו** | "ויסקי סאוור עם חלבון **ביצה**" מול האופציה "לשייק משקה עם הדר, **ביצה** או שמנת" | שער חפיפת-מילה (כלל 4) — נכשל כשבדיוק אופציה אחת חולקת מילה עם השאלה **והיא הנכונה**. ⚠️ **השער מכייל את עצמו מהבנק**: מילה שמופיעה ביותר מעשירית מהשאלות ("אורח", "מנה", "שולחן") היא רקע. רשימת עצירה ידנית מתיישנת ברגע שהבנק גדל | `WORD-OVERLAP` |
| 21 | **גיוון מדומה** — `kind` שונה, מסך זהה | `first` ו-`choice` הן שתיהן ארבע אופציות; כלל הקצב חשב שהוא גיוון ביניהן | קצב נמדד לפי **צורת התשובה** (`shapeOf`: single/multi/order) — מה שהמלצר עושה פיזית — ולא לפי התווית הסמנטית | `RHYTHM` |
| 22 | **מסיח שהתפקיד הזה לא לומד** | ברמן שמקבל "לקפל מחדש את המפית" פוסל אותה בלי לדעת כלום על בר | `ELIMINABLE` מוכיח שהפעולה אמיתית, לא שהיא אמיתית **לאדם הזה**. מסיח חייב להגיע ממודול שהתפקיד לומד | `ROLE-DISTRACTOR` |

### סמכות: מי קובע מה התשובה הנכונה בשאלת שירות

⚠️ **הכלל החשוב ביותר בקבוצה הזו.** "מאיזה צד מגישים?" משתנה בין מסעדות. אם נקבע אנחנו,
אנחנו מלמדים מלצר משהו **שגוי על מקום העבודה שלו** — בדיוק הכשל ש-`qServingOrder` נועד
למנוע ("המסעדה היא הסמכות היחידה על סדר ההגשה שלה").

שלוש שכבות ב-`serviceStandard.js`:

| שכבה | סמכות | במבחן? |
|---|---|---|
| **A** אוניברסלי (`serviceBank.js`) | תקן מקצועי | תמיד |
| **B** סטנדרט בית (`HOUSE_KEYS`) | **הבעלים** | **רק אחרי אישור** |
| **C** מקושר-תפריט (`serviceScenarios.js`) | נתוני המסעדה | תמיד |

**שכבה B נשלחת עם ברירת מחדל מקצועית ממולאת מראש** (פנקס השותף #8 — המערכת מציעה,
המנהל מאשר), אבל `confirmed=false` ⇒ **מתרגלים עליה ולא נבחנים בה**. הסיבה: המבחן הוא
מה שהבעלים קורא כדי להחליט אם מישהו יכול לעבוד על הרצפה, והוא לא יכול להפוך **ניחוש
שלנו** לסטנדרט **שלו**. בתרגול טעות עולה ניסיון חוזר; בהסמכה היא עולה החלטה.

### מבנה המבחן — למה "קשה מדי ולא ממוקד"

| היה | הכשל | עכשיו |
|---|---|---|
| שעון אחד ל-40 שאלות | מי שחשב לעומק בשאלה 3 מפסיד בשאלה 30 — נמדדת אסטרטגיית קצב | **שעון לכל שאלה** (25/40/60ש'), בלי גלגול זמן |
| 40 שאלות מאותו טעם | מעל ~20 נמדד מי עדיין קורא | **23 שאלות** בשש קטגוריות (~10 דק') |
| אחוז אחד | 72% יכול להיות "סביר בכל דבר" או "מצוין בכל דבר וריק באלרגיות" | **ציון פר-קטגוריה** + הקטגוריה החלשה ביותר בשם |

⚠️ **רצפת בטיחות** (`SECTION_PLAN[S4].floor = 80`): מעבר דורש גם ציון כולל **וגם** 80%
באלרגיות. מלצר יכול להיות מצוין בכל השאר ועדיין אסור להסמיך אותו כשהוא מנחש בשדה היחיד
במוצר הזה שיכול לגמור באמבולנס. יש בדיקה שמריצה ישיבה מושלמת עם 1/5 באלרגיות ומוודאת
שהיא **נכשלת**, ואת ההפך — שישיבה מושלמת **עוברת** (שער שחוסם חייב להוכיח שהוא מעביר).

⚠️ **ריצה חוזרת אינה אותה ריצה** — `RETAKE-IDENTICAL` נכשל אם חפיפה >70%, אחרת המבחן
השני מודד זיכרון של האפליקציה.

## טעויות קוד (לא תוכן) שנתפסו

| # | הטעות | הכלל |
|---|---|---|
| A | מסך תוצאה שקורא `result.pct` לפני ש-`finish()` האסינכרוני רץ ⇒ קריסה | כל מסך "תוצאה" חייב `if (!result) return <spinner/>` לפני הרינדור. אל תניח ש-effect רץ לפני render |
| B | `finish()` נקרא פעמיים ב-StrictMode ⇒ ציון נשמר כפול | דגל `useRef` סביב כל side-effect חד-פעמי שמופעל מ-`useEffect` |
| C | **UPDATE נחסם ב-RLS ⇒ 0 שורות, בלי שגיאה.** בוחן ההיכרות "נשמר" בהצלחה אבל `baseline_pct` נשאר NULL | ב-`menu_app` צריך **שני** מנגנונים להתיישר: `GRANT UPDATE (cols)` קובע *אילו עמודות*, ו-policy קובע *אילו שורות*. חסר policy = כישלון שקט. **תמיד לבדוק `error` מכל כתיבה** ולהציג משהו למשתמש — אל תבלע |
| D | `upsert` עם אותו מפתח פעמיים באותו payload ⇒ `ON CONFLICT ... cannot affect row a second time`, כל הבאטץ' נופל | לדדפ לפי מפתח הקונפליקט לפני שליחה. קורה בטבעיות כשה-deck שואל על אותה מנה בשתי שאלות |
| E | שאילתה בלי `ORDER BY` ⇒ "סדר התפריט" משתנה בין טעינות, ומסלול הלמידה מתערבב | כל שאילתה שסדרה נראה למשתמש חייבת מיון מפורש **וטוטאלי** (עמודה + tiebreak ייחודי). `synced_at` אינו סדר תפריט — הוא משתנה בכל עריכה |
| F | בחירה מרובה שנשארה תקועה אחרי שהרשימה התכווצה ⇒ "נבחרו 3 מתוך 1" | אחרי פעולה שמשנה את הרשימה — לגזום את הבחירה לפריטים שעדיין קיימים (`useEffect` על הרשימה), לא להשאיר ids מתים |
| G | תווית קטגוריה מיובאת נדחפת לתוך משפט ⇒ "נפתח אחרי שעוברים את המבחן של מאקי — 6 יחידות, אצה בחוץ ואורז בפנים" | בטקסט רץ להשתמש ב-`shortCat()` (החלק לפני המקף), לא בתווית המלאה. התווית המלאה רק ככותרת עצמאית או כאופציה במבחן |
| H | רשימת אלרגנים שונה בין האפליקציות (10 מול 9) ⇒ למלצר מוצע "סולפיטים" שאף בעלים לא יכול לסמן, כלומר תשובה שתמיד שגויה בלי דרך ללמוד למה | רשימת האלרגנים זהה בשלושת המקומות: `menu-ai-parse`, אפליקציית הבעלים, אפליקציית המלצרים |
| I | תיקון גורף שהוחל על כל השמות במקום רק על הבעייתיים ⇒ בתפריט היווני המנות הוצגו כ-"mains Mykonos Tuna" — מפתח הקטגוריה באנגלית הודבק לשם שמעולם לא היה דו-משמעי | `withDisplayNames` מסווג **רק** שמות שמופיעים ביותר מקטגוריה אחת. הכלל הכללי: תיקון שנועד למקרה קצה חייב לחול רק על מקרה הקצה, אחרת הוא באג חדש בכל שאר המקרים |
| J | **שער איכות שחסם שאלה תקינה** — `nearDuplicateOptions` השווה אופציות אחרי `norm`, ש**מסנן מילים באורך 1**. אופציות מספריות ("5", "3") הפכו לסטים ריקים, ו-`jaccard(∅,∅)=1` ⇒ כל שאלת כמות נדחתה בשקט | כששני צדדים מנרמלים לריק, אין חפיפה למדוד — להשוות את המחרוזות עצמן. **שער שדוחה בשקט חייב בדיקה שמוכיחה שהוא מעביר את המקרה התקין**, לא רק שהוא חוסם את הפסול |
| J | `Number(null) === 0` והוא `isFinite` ⇒ מלצר בלי מבחן היכרות הוצג כ"התחיל ב-0%" וקיבל קרדיט על שיפור מדומה | לבדוק `x != null` **לפני** `Number.isFinite`. שדה חסר הוא לא אפס — הוא "לא ידוע", והתצוגה צריכה להגיד את זה |
| K | שער איכות שחוסם לגמרי במקום להחליף מסלול ⇒ קטגוריה בלי מרכיבים לא יכלה להיבחן, וכל המסלול נתקע מאחוריה לנצח | שער שחוסם **צעד הכרחי בהתקדמות** חייב fallback, לא רק `disabled`. שאל תמיד: "אם התנאי לא יתקיים אף פעם, המשתמש תקוע?" |

## שערי איכות אוטומטיים (`validateQuestion`)

כל שאלה שיוצאת מכל builder עוברת דרך `validateQuestion` לפני שהיא נכנסת ל-deck.
נכשלה → ה-builder מחזיר `null` וה-deck בונה שאלה אחרת במקומה. השערים:

1. `correct` נמצא ב-`options` — שגיאת תכנות, לא תוכן
2. אין אופציות כפולות/כמעט-כפולות (Jaccard ≥ 0.85 על מילות תוכן)
3. אין חפיפת-מילה חד-משמעית בין הנושא לאופציה אחת בלבד
4. אורך הנכונה בטווח סביר מול חציון המסיחים
5. לפחות 3 אופציות
6. לנושא נשאר תוכן אמיתי אחרי מיסוך (≥3 מילות תוכן)

## כשמוסיפים סוג שאלה חדש

1. כתוב את ה-builder כך שיחזיר `null` בכל מצב שבו הנתונים לא תומכים בשאלה הוגנת
2. הרץ `node tests/engine.test.mjs` — הוא בודק את כל סוגי השאלות מול 5 תפריטים:
   סושי (38 מנות), יווני (19), מיני (3), קטגוריות-קורס (שער ידע כללי), ומחירים-בשם
3. **שחק את המשחק בפועל בדפדפן** לפני commit — חלק מהבעיות (תבנית ויזואלית,
   שאלה שמרגישה טיפשית) לא נתפסות בבדיקה אוטומטית
4. אם מצאת סוג טעות חדש — הוסף שורה לטבלה למעלה **ובדיקה** שנכשלת עליה

## סבב לימוד — `src/lib/studySession.js` (2026-08-13)

לא שאלה, אבל אותו עיקרון: **מה שמוצג למלצר נגזר מהביצועים שלו, לא מהתפריט המלא.**

| כלל | למה | בדיקה |
|---|---|---|
| מקסימום 10 מנות בסבב | קטגוריה של 22 מנות פתחה את כל ה-22 בכל פעם — שוחק, ורוב הזמן מושקע במה שכבר יודעים | `SIZE` |
| 5 פעמיים ברצף ⇒ המנה יוצאת מהרוטציה | 5 אחד יכול להיות מזל או הצצה; פעמיים ברצף זה אישור | `RETIRE` |
| חלש קודם, וגם יותר פעמים | "האלה שאתה פחות מצליח מופיעים יותר וגם יותר בהתחלה" | `ORDER`, `REPEAT` |
| חזרה רק למנה שדורגה **נמוך** (≤2), לא למנה שלא נראתה | אי אפשר "להיכשל" במנה שעוד לא ענית עליה — לתרגל אותה פעמיים לפני תשובה ראשונה זה רעש | `REPEAT` |
| קטגוריה שכולה בשליטה חוזרת כרענון | פרישה נועדה לקצר סבב, לא לסיים לימוד | `RETIRE` |

⚠️ `mastery` הוא **הדירוג האחרון בלבד** ולכן לא יכול לבטא "הצליח פעמיים ברצף" —
בשביל זה נוספה `menu_progress.consecutive_fives` (5 מעלה, כל דירוג נמוך יותר מאפס).

```bash
node tests/session.test.mjs
```
