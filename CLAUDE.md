# shiftcrew-waiter — אפליקציית לימוד לצוות

> ר' גם `/Users/homestation/Desktop/CLAUDE.md` להקשר הכללי (Supabase, deploy, סטטוס,
> ובמיוחד "סטטוס 2026-08-11" — commits בשם `Eytan Shleizer` הופיעו ב-`origin/main`
> תוך כדי עבודה על הריפו הזה, ראו שם דיון אם זו זהות git מקומית או session מקביל).
> קובץ זה מתמקד בפרטים הספציפיים לאפליקציה הזו — **כולל תיקייה מלאה בקוד מת/דמו,
> קרא את "שלדים מהעבר" לפני שאתה נוגע בקבצים שאינך מכיר.**

## מה זה

אפליקציית web לצוות המסעדה — כניסה בקוד צוות (`1234`) + שם (ללא סיסמה), לימוד תפריט
בסגנון Quizlet, Daily Brief, לידרבורד live, התקדמות באחוזים לפי קטגוריה.

## ⚠️ קרא קודם: `QUESTION-QUALITY.md`

**חובה לפני כל שינוי במנוע השאלות או במצב משחק.** זה קטלוג כל טעות שנתפסה בפרויקט —
שאלות שנענות בלי ידע התפריט, כשלים שקטים של RLS, שערים שנועלים את המשתמש — כל אחת עם
הכלל שמונע אותה והבדיקה שנכשלת אם היא חוזרת. **אל תמחק ממנו שורות.**

```bash
node tests/engine.test.mjs && node tests/path.test.mjs
```

## מסלול הלמידה המדורג (2026-08-12)

> ⚠️ **הסעיף הזה עודכן 2026-08-13 — המודל הישן ("שרשרת" של קטגוריות נעולות) כבר לא
> קיים.** אם אתם קוראים כאן משהו שסותר סעיף מאוחר יותר בקובץ, הסעיף המאוחר גובר.

```
לוגיקה טהורה (בלי React, נבדקת ב-node):
  src/lib/questionEngine.js — בניית שאלות + שערי איכות + facets
  src/lib/learningPath.js   — מה בהיקף התרגול ומה השלב המומלץ (אין נעילות)
  src/lib/studySession.js   — בניית הסבב: גודל, פרישה, עדיפות, STAR_BOOST
  src/lib/studyTime.js      — ספירת זמן לימוד + סנאפשוטים לגרף של הבעלים

מסכים:
  src/screens/BaselineIntake.jsx — דירוג עצמי בכתב ⇒ מבחן מתוזמן ⇒ "ידע התחלתי X%"
  src/screens/MainApp.jsx        — בית + 5 הטאבים (~1150 שורות אחרי החילוץ)
  src/games/                     — ⭐ משחק לקובץ + shared.js לעזרים המשותפים
  src/components/                — MetricsScreen · BriefAck
```

- **אין נעילות.** כל קטגוריה פתוחה ללימוד ולמבחן בכל סדר, וכל מצבי התרגול פתוחים
  מההתחלה. מה שמבחן משנה זה **היקף** התרגול (`path.gamePool`), לא גישה —
  ר' "🔓 מסלול פתוח" בהמשך.
- **מבחן** נפתח בסף של הבעלים (ברירת מחדל 50%, `exam_config.pass_threshold`) —
  זה הגייט היחיד שנשאר, והוא רק אומר "תלמדו קצת לפני שתיבחנו".
- **שני פורמטים למבחן סיום, אותו חוזה**: `CategoryExam` (צ'יפים, בחירת סט מרכיבים מדויק)
  כשיש מרכיבים ובעלים שמעוניין בהם; אחרת `QuizExam` (בחירה מרובה מה-facets של הבעלים).
  ⚠️ **קטגוריה חייבת תמיד להיות ניתנת למעבר** — אחרת כל המסלול נתקע מאחוריה (ר' טעות K).
- **`exam_config`** (טבלה, נערכת באפליקציית הבעלים) קובעת: על מה בוחנים ובאיזה סדר
  חשיבות (`facets`), סדר הקטגוריות, הסף, אם יש פתיחה מדורגת, ואם יש מבחן היכרות.
  כל המשחקים בונים deck דרך `buildWeightedDeck(pool, n, facets)` — **אף מצב לא מחזיק
  רשימת builders משלו**, אחרת דירוג הבעלים לא היה משפיע עליו.

**Live**: https://shiftcrew-waiter.vercel.app
**Repo**: github.com/ezergalit/shiftcrew-waiter (branch `main`, Vercel auto-deploy)

## ⚠️ שלדים מהעבר — קבצים שקיימים אבל לא בשימוש

התיקייה עברה כמה פיבוטים (scheduling app מקורי → דמו יווני סטטי → Menu Trainer עם DB אמיתי).
כתוצאה יש כמה קבצים "רדומים" ש-**נראים** כמו האפליקציה החיה אבל אינם:

| קובץ | מה זה באמת | מסקנה |
|---|---|---|
| `src/RestaurantDemo.jsx` | wrapper דק סביב `RestaurantApp.jsx` | **לא בשימוש.** `main.jsx` היה מצביע עליו בטעות עד 2026-08-10 — זו הייתה הסיבה שהאפליקציה החיה מעולם לא רצה כמו שציפו (אין login אמיתי, אין DB, matching/speed מציגים "בקרוב"). תוקן: `main.jsx` מצביע עכשיו על `App.jsx` האמיתי. |
| `src/screens/RestaurantApp.jsx` | דמו סטטי עם state בזיכרון בלבד, כניסה בשם+טלפון (לא team_code), sidebar אנכי צדדי (לא bottom nav) | **לא בשימוש בפרודקשן**, אבל **שימושי כמקור** — זה המקום שבו נמצא התפריט האמיתי (`GREEK_MENU`, 19 מנות עם שמות ומחירים מאומתים) ששימש בסיס לזריעת ה-DB. אם צריך לאמת שוב פרטי מנה — תסתכל כאן קודם. |
| `src/lib/shiftcrew.js` | שכבת גישה ל-DB לפרויקט ה-scheduling המקורי (`scWaiter`/`scOwnerPublic`, סכמאות `shiftcrew_owner`/`shiftcrew_waiter`, RPC כמו `waiter_access`, `publish_week`) | **סכמות שלא קיימות יותר.** לא בשימוש. `MainApp.jsx` ניסה בעבר לייבא ממנו `supabase` (לא קיים שם בכלל) — זה היה באג נוסף שגרם לקריסה. תוקן: `MainApp.jsx` מייבא מ-`lib/supabase.js` החדש. |
| `src/auth/WaiterLogin.jsx` | login ישן מבוסס טלפון | לא בשימוש (הוחלף ב-`TeamLogin.jsx`) |
| `src/screens/Manage.jsx`, `src/screens/Tutorial.jsx` | מסכי ניהול/הדרכה מהגרסה הישנה (scheduling, product tour) | לא בשימוש, לא מיובאים מ-`App.jsx` |
| `src/Demo.jsx` | עוד דמו ישן | לא בשימוש |

**כלל אצבע**: האפליקציה החיה היא אך ורק השרשרת `main.jsx → App.jsx → auth/TeamLogin.jsx |
screens/WelcomeTutorial.jsx | screens/MainApp.jsx`. כל קובץ אחר ב-`src/` הוא ארכיון/
מקור-נתונים, לא קוד רץ. לפני שמתקנים באג "באפליקציה" — ודא קודם שאתה בקובץ הנכון בשרשרת הזו.

## זרימת האפליקציה (הקוד החי)

```
main.jsx → App.jsx
              │
              ├─ טוען session מ-localStorage ("menu-app-team-session")
              │   ├─ אין session → TeamLogin
              │   └─ יש session → מוודא team_members.id קיים ב-DB →
              │       ├─ session.showTutorial === true → WelcomeTutorial (פעם אחת בלבד)
              │       └─ אחרת → MainApp
              │
TeamLogin.jsx (שוכתב 2026-08-11 — חשבונות אמיתיים):
  מזין team_code + שם פרטי **ושם משפחה** (שניהם חובה, `first_name`/`last_name`) →
  מחפש restaurant לפי team_code → שולף את כל חברי הצוות של אותה מסעדה →
  1. התאמה מדויקת (case-insensitive) → משוחזר בשקט, בלי לשאול
  2. אין מדויקת אבל יש שם קרוב (Levenshtein distance ≤2, פונקציה מקומית `levenshtein`) →
     מסך אישור "האם אתה/את X?" (`pendingMatch` state) — "כן" משחזר את הפרופיל הקיים,
     "לא" יוצר חבר צוות חדש עם השם שהוקלד. זה בדיוק המקרה "יותם עזר"/"יותם אזר" מהבקשה —
     מונע גם טעויות הקלדה מקריות וגם התחזות בלי אישור מפורש.
  3. אין התאמה בכלל → יוצר `team_members` חדש
  נבדק חי קצה-לקצה כולל שני הענפים (כן/לא) מול ה-DB האמיתי.
  שולף גם `service_style`/`service_notes` (בנוסף ל-`description`/`cuisine_types`) ומעביר
  ל-session — מזין את מסך ה-WelcomeTutorial. מסמן `showTutorial: true` **רק כשנוצר
  פרופיל חדש** (לא כשמשחזרים קיים / מאשרים fuzzy-match כ"זה אני").

WelcomeTutorial.jsx (חדש 2026-08-11 — בקשת משתמש):
  4 שקופיות, מוצג פעם אחת לכל חבר צוות חדש, לפני MainApp:
  1. **תדריך על המסעדה** — שם + `cuisine_types` + `description` + `service_notes`,
     כלומר **מה שבעל המסעדה כתב באונבורדינג באפליקציית הבעלים** (`menu_app.restaurants`).
     אם השדות ריקים ב-DB → נופל להודעה גנרית. ⚠️ **זה לא באג** — התוכן הזה מגיע מהבעלים,
     אז מסעדה שלא השלימה אונבורדינג תראה מסך רזה (נכון ל-2026-08-11 זה המצב של SALON
     האמיתית: `description`/`service_notes`/`cuisine_types` כולם ריקים).
  2. איפה כל דבר — הסבר על 5 הטאבים בניווט התחתון
  3. איך לומדים — ההבדל בין Flashcards (self-report) לשאר המשחקים (אובייקטיביים)
  4. סיום
  `App.jsx` מכבה את הדגל (`showTutorial: false`) ומעדכן את localStorage בסיום/דילוג,
  כך שהוא לא חוזר ברענון. אין עמודת DB לזה — הדגל חי רק ב-session המקומי, כלומר
  מכשיר חדש יראה את הטוטוריאל שוב (מקובל, לא נדרש אחרת).

MainApp.jsx (src/screens/MainApp.jsx):
  5 טאבים בניווט תחתון (עם label, לא רק אייקון): בית | אתגרים | יומי | דירוג | תפריט
  (הטאב האחרון עדיין נקרא "categories" בקוד/state — רק התווית שונתה ל"תפריט" ב-2026-08-10,
  התוכן עצמו נשאר פירוט התקדמות לפי קטגוריה, לא רשימת המנות המלאה)

  Home tab מתחיל בקרוסלת פרסומות (PromoCarousel) — כרטיס אחד בכל פעם, מתחלף אוטומטית
  כל 4.5 שניות (fade, לא slide), ניתן גם להחליק (touch) או ללחוץ נקודה. תוכן: אתגר יומי,
  שיא-רצף של חבר צוות אחר (leaderboard.streak), מוביל/ה בניקוד, וטיזרים למשחקי match/speed.
  לחיצה על כרטיס מנווטת ישירות למצב המתאים. ⚠️ **ר' "מלכודת RTL" למטה — קריטי לכל
  קרוסלה/swiper עתידי באפליקציה.**
  8 מצבי משחק (⚠️ **חיים ב-`src/games/`**, לא בקובץ הזה — מופעלים מ"בית", מטאב "תפריט"
  או מהקרוסלה, עם items מלא או scoped דרך `modeItems`; `quick` = סבב 5 הדקות):
    כרטיסיות | חידון | התאמה | מהירות | אלרגיות | התאימו תיאור למנה | מבחן קטגוריה

  ⭐ מודל הציונים (עודכן 2026-08-10, שינוי משמעותי): `mastery` הוא 1-5, `>=4` = "נלמד".
  **רק Flashcards הוא self-report** (המשתמש בוחר 1-5 בעצמו אחרי reveal — אין דרך
  אובייקטיבית לבדוק "האם ידעת להסתכל על כרטיס"). **כל שאר המצבים אובייקטיביים** —
  המשחק עצמו קובע את הציון לפי תשובה נכונה/שגויה, כדי שאי אפשר "לשקר" ולסמן שידעת
  בלי להיבדק בפועל:
  - Quiz: תשובה נכונה→5, שגויה→2
  - Speed: **שוכתב פעמיים** — היה self-report ("ידעתי"/"לא יודע", באג בפני עצמו), ואז
    multiple-choice על המחיר (3 ברירות, לקצב מהיר) תחת שעון 30 שניות כללי — **שניהם
    הוחלפו 2026-08-11** (ר' "מחיר הוסר מכל המשחקים" למטה); נכון→5, שגוי→2
  - Matching: נכון→5, אבל **כל ניחוש שגוי על אותו pairId מוריד את הציון** בהתאמה
    הסופית (0 שגיאות=5, 1=4, 2=3, 3+=2) — נספר לכל צד של הזוג דרך `wrongAttemptsRef`
    (Map פר-pairId, לא state, כדי לא לגרום re-render על כל ניחוש)
  - אלרגנים / השלמת שם (חדשים, ר' למטה): נכון→5, שגוי→2
  ⚠️ **שם מנה מוצג עם סגנון ההגשה (`displayName`, 2026-08-12 — בקשת משתמש)**: בתפריט
  סושי השם החשוף הוא לא מנה. בתפריט אמיתי אחד **6 שמות מופיעים פעמיים** תחת סגנונות
  שונים ("סלמון אבוקדו" = מאקי *וגם* אינסייד אאוט; "בס" = סשימי *וגם* ניגירי). בלי
  הסיווג שתי אופציות באותה שאלה נקראות זהה כשרק אחת נכונה, ו-`qWhichDish` היה **מוריד
  את השאלה בשקט** כי ה-Set מיזג את שני השמות הזהים.
  `pubToCard` מוסיף `displayName = "<סגנון> <שם>"` (הסגנון = החלק לפני ה-em dash בשם
  הקטגוריה), וכל מקום שמציג מנה עובר דרך `dishLabel(it)` (מיוצא מ-`questionEngine.js`).
  **`name` נשאר חשוף בכוונה** — המנוע ממסך תיאורים מולו (`maskNameLeak`), ותיאור אף פעם
  לא מכיל את סגנון ההגשה; מיסוך מול השם המסווג היה מוחק גם את מילת הסגנון מהתיאורים.
  📌 **`qServingStyle`** (חדש): "באיזו הגשה מוגשת המנה?" — האופציות הן **שורות הקטגוריה
  המלאות**, שבתפריט מיובא נושאות את מספר היחידות ("מאקי — 6 יחידות", "אינסייד אאוט — 8
  יחידות", "סשימי — דק (5 פרוסות) / עבה (3 פרוסות)"). כלומר תשובה נכונה = לדעת את
  מספרי היחידות, בלי שום parsing של המספרים מהטקסט. ה-subject שם הוא **השם החשוף**
  (המסווג היה מסגיר את התשובה), והשאלה מדלגת על מנות ששמן משותף לכמה סגנונות — אין
  ניסוח שיכול להבחין ביניהן.

  ⚠️ **קטגוריות נגזרות מהתפריט, לא מרשימה קבועה (תוקן 2026-08-12 — באג פרודקשן)**:
  `cats` היה `["starters","mains","desserts","drinks"].map(...)`, כלומר סינון מול ארבעה
  מפתחות אנגליים קשיחים. אבל אפליקציית הבעלים מייצרת קטגוריות **טקסט חופשי בעברית**
  (ייבוא תפריט/AI), ולכן **לכל מסעדה שבנתה תפריט דרך הייבוא טאב "תפריט" היה ריק לגמרי** —
  ובלי שורות קטגוריה גם אין גישה למבחן. זה כבר פגע בפרודקשן: "studio" ו"סטודיו תל אביב"
  (37 מנות כל אחת) עם קטגוריות כמו "מאקי — 6 יחידות, אצה בחוץ ואורז בפנים".
  עכשיו `cats` נגזר מ-`cards` עצמם (`[...new Set(cards.map(x => x.category))]`), עם
  `CAT_ORDER` לשמירת הסדר הקנוני של המפתחות הישנים ושאר הקטגוריות אחריהם בסדר התפריט.
  התוויות עוברות דרך `catLabel(c) = CAT_LABELS[c] || c` — **אף פעם לא `CAT_LABELS[c]` ישיר**,
  אחרת שם עברי חופשי היה מרונדר כ-undefined. שמות ארוכים מקבלים `line-clamp-2` + `title`.

  ⭐ **התקדמות באחוזים (2026-08-11, בקשת משתמש — שינוי משמעותי)**: אחוז ההצלחה מחושב
  עכשיו מ**סכום הציונים** ולא מספירת מנות שעברו סף. `scorePct(list) = sum(mastery) /
  (list.length * 5)`. כלומר 4/5 בכל המנות = **80%**, לא 100%. `mastered` (הסט של >=4)
  עדיין קיים ומזין נקודות/אתגר-יומי/leaderboard, אבל **האחוזים לא נגזרים ממנו**.
  ה-state שמחזיק את זה הוא `masteryById` (id → 1-5), נטען מ-`menu_progress` ומתעדכן
  בכל `learnItem`. חל גם על סרגל ההתקדמות בבית וגם על כל קטגוריה בטאב "תפריט".

  learnItem(id, rating) ב-MainApp מטפל בהכל: **mastery יכול לזוז גם למטה** — פריט
  שכבר "נלמד" יכול "להתבטל" אם עונים עליו לא נכון במשחק אובייקטיבי מאוחר יותר (בכוונה
  — זו בדיוק הנקודה של "המשחק מחליט, לא רק אתה"). upsert תמיד ל-menu_progress; upsert
  ל-leaderboard ועדכון האתגר-היומי/בונוס **רק כשעוברים סף (4)** לכיוון כלשהו, לא בכל
  ציון. realtime subscription (postgres_changes על leaderboard) משדר לכל הצוות.

  - Flashcards: הצג שם (+ טיזר "X מרכיבים · Y אלרגנים" אם קיימים) → reveal → תיאור
    מלא+מרכיבים+אלרגנים → **בוחרים 1-5** ("כמה טוב ידעתם?")
  - Quiz: **שוכתב שוב 2026-08-12 על מנוע השאלות החכם** (ר' סעיף `questionEngine.js`
    למטה) — deck מעורב של 8 שאלות מ-4 סוגים: תיאור-ממוסך→שם, "איזה מרכיב לא נמצא
    במנה?", "אילו שינויים ניתן לעשות במנה?", שם→תיאור-ממוסך. פידבק ירוק/אדום, 1100ms
  - Matching (בסגנון Quizlet): **שוכתב 2026-08-11** — היה שם↔מחיר, עכשיו שם↔מרכיבים
    (עד 3 מרכיבים ראשונים, מחוברים בפסיק). גריד 3 עמודות, 6 מנות (מסונן ל-`it.ingredients
    ?.length > 0`) × 2 tiles מעורבבים, טיימר עולה — ר' מודל הציונים למעלה
  - Speed: **שוכתב 2026-08-11** — היה multiple-choice מחיר, עכשיו "איזה מרכיב שייך
    למנה הזו?" (3 ברירות: המרכיב הנכון + 2 distractors ממנות אחרות). 30 שניות, עד
    12 מנות (מסונן ל-`it.ingredients?.length > 0`) — מדווח ציון סופי ל-MainApp דרך
    onFinish (לאתגר "שיא מהירות", נפרד מה-mastery של כל שאלה)
  - **אלרגנים** (AllergenQuiz, חדש): שם המנה בלבד (לא מחיר/תיאור) → בוחרים chips
    מתוך 10 אלרגנים אפשריים (אותה רשימה כמו ב-owner app) → "שליחה" (שליחה בלי לבחור
    כלום = תשובת "אין אלרגנים" בעצמה) → צריך התאמה **מדויקת** של הסט (לא ניקוד חלקי)
  - **התאימו תיאור למנה** (קומפוננטה עדיין נקראת `NameCompletion` בקוד, מצב `namecomplete`
    — שוכתב 2026-08-11 ל-tap-only, ו-**שוב 2026-08-12 על מנוע השאלות החכם**): deck
    מעורב של 8 שאלות מ-3 סוגים (תיאור-ממוסך, שינויים, מרכיב-שלא-במנה), כולן בפורמט
    "שם המנה למעלה, 3 אופציות". ההיסטוריה: קלט חופשי (הקלדת שם) בוטל 2026-08-11 כי
    שמות המנות באנגלית/תעתיק והקלדה מדויקת בדקה איות ולא ידע. Tap-only, אין הקלדה.

  ⭐ **מנוע השאלות החכם — `src/lib/questionEngine.js` (2026-08-12, משוב משתמש)**:
  "רוב השאלות היו קלות וברורות" — המקרה המובהק: המנה "סלמון אבוקדו" הציגה כתשובה
  נכונה את התיאור "סלמון ואבוקדו…" — השאלה ענתה על עצמה. המנוע (מודול טהור, בלי React,
  ניתן לבדיקה ב-node) פותר את זה בשלוש דרכים, כולן דטרמיניסטיות ועובדות על כל תפריט
  (אין AI בזמן-ריצה — ההחלטה הארכיטקטונית הקיימת בעינה):
  1. **מיסוך הדלפות** (`maskNameLeak`): כל מילה משם המנה שמופיעה בטקסט מוצג מוחלפת
     ב-▢▢▢, כולל תחילית עברית אחת ([ובלהמשכ]). המיסוך אחיד על **כל** האופציות —
     כולל התאמת "מסיכתיות" ב-qChanges (מסיחים עם ▢ כשלנכונה יש ▢) — כדי שהמיסוך
     עצמו לא יסמן את התשובה. שאלה שהמיסוך מרוקן אותה (נשארו <3 מילים) מדולגת.
     ⚠️ מגבלה ידועה: שם באנגלית מול תיאור בעברית (Sea Bass ↔ "בס ים") לא ממוסך —
     תעתוק בין כתבים לא מטופל.
  2. **מסיחים לפי דמיון** (`pickDistractors`, עבר לכאן מ-MainApp): אותה קטגוריה קודם
     (כמו קודם), אבל עכשיו ממוין לפי דמיון — מרכיבים משותפים ×2 + מילות תיאור
     משותפות, עם jitter אקראי קטן. משרת גם את Speed דרך ה-import.
  3. **שאלות עובדה מהטקסט של הבעלים**: `splitChanges` מפרק את קונבנציית "שינויים: …"
     שכותב ייבוא ה-AI → שאלת "אילו שינויים ניתן לעשות במנה?" (למנה בלי זנב שינויים,
     כשלאחיות יש — התשובה הנכונה היא "אין שינויים במנה זו"); ו"איזה מרכיב לא נמצא
     במנה?" — 3 מרכיבים אמיתיים (מעדיפים כאלה שלא בשם המנה) + פולש מהמנה הדומה ביותר.
  כל builder בודק לבד אם יש לו נתונים ומחזיר null אחרת — `buildSmartDeck` עושה
  רוטציה בין ה-builders פר-מנה, כך שכל תפריט (עם/בלי מחירים/תיאורים/שינויים) מקבל
  את המיקס הקשה ביותר שהוא יכול לשאת. נבדק אופליין (node) על תפריט הסושי מהגיבוי,
  התפריט היווני, ותפריט של 3 מנות: 0 דליפות, 0 כפילויות, correct תמיד שייך למנה;
  ונבדק חי ב-Quiz מלא (7/8 עם טעות מכוונת אחת, ציונים נרשמו נכון ב-menu_progress).

  **מחיר הוסר מכל המשחקים (2026-08-11, משוב משתמש)**: `it.price` עדיין קיים במודל
  הנתונים (`pubToCard`) אבל אף קומפוננטת משחק לא מציגה או משתמשת בו יותר — לא Flashcards,
  לא Quiz/Speed/Matching (ששלושתם היו מבוססי-מחיר לפני זה). הסיבה: מחיר לא רלוונטי
  לידע על התפריט, וחלק משמות המנות בדאטה הנוכחית כוללים את המחיר בתוך השם עצמו
  (`Sea Bass 165`, `Greek Truffle Cream 38/44/48` — ר' "התאמת התפריט" ב-CLAUDE.md
  הראשי) — כלומר משחק מבוסס-מחיר יכול היה "לדלוף" את התשובה דרך השם. השלושה עברו
  ל-ingredients/desc: Quiz→תיאור-לשם, Matching→שם↔מרכיבים, Speed→שם→מרכיב נכון.

  - **מבחן קטגוריה** (`CategoryExam`, מצב `exam`, 2026-08-11): שלב הסיום של קטגוריה.
    **בחירה מרובה מקובעת, לא טקסט חופשי.** מציג שם מנה + צ'יפים של מרכיבים: המרכיבים
    האמיתיים מעורבבים עם **מסיחי near-miss מהמנות האחרות באותה קטגוריה**, ועוד בחירת
    אלרגיות מתוך `ALLERGENS`. הנבחן חייב לסמן בדיוק את הסט הנכון.
    ניקוד **Jaccard** — `נכונים / (נכונים + פספוסים + שגויים)` — 60% מרכיבים + 40%
    אלרגיות. סט ריק מול סט ריק = 1 (לדעת שאין אלרגיות זה ידע אמיתי; לסמן אלרגיה שלא
    קיימת מוריד ניקוד). ממופה ל-mastery 1-5 כמו כל מצב אובייקטיבי. **נעול עד 50%**.
    ⚠️ **למה זה הוחלף — באג עיצובי אמיתי שנתפס בבדיקה**: הגרסה הקודמת הייתה טקסט חופשי
    שנוקד לפי כמה מהמרכיבים האמיתיים הוזכרו. זה מדד **recall בלבד ולא precision**, ולכן:
    (א) תשובה שמונה את *כל* המרכיבים בקטגוריה קיבלה **100% על כל מנה**, (ב) אי אפשר היה
    להבחין בין Greek Truffle Cream 38/44/48 — בדיוק ההבחנה שחשובה בשירות, (ג) תיאור של
    מנה אחרת לגמרי קיבל 40% רק בזכות אזכור האלרגן. נמדד חי: "בחר הכל" ירד מ-100% ל-34%.
    ⚠️ **החלטה ארכיטקטונית של המשתמש**: אין AI בבדיקה. גרסה שתוכננה שלחה כל תשובה
    ל-Claude — נפסלה כי זו קריאת API לכל תשובה × כל מלצר × לנצח. אם בעתיד רוצים שכבת AI,
    **המקום הוא אפליקציית הבעלים, פעם אחת בבניית התפריט** (לייצר מסיחים/מפתח בדיקה
    שנשמרים ב-DB ומשרתים את כל המלצרים), לא בזמן אמת בצד המלצר.
    📊 **רישום מבחנים (טבלה `menu_app.exam_results`, נוסף 2026-08-12)**: בסיום כל מבחן
    נכתבת שורה אחת — `restaurant_id`, `team_member_id`, `category` (המפתח, לא התווית),
    `score`, `passed`, `dish_count`, `taken_at`. הכתיבה דרך `recordExam` ב-MainApp
    (ל-CategoryExam אין גישה ל-session/db, אז הוא מדווח החוצה ב-`onFinish`). הטריגר הוא
    `useEffect` עם `reportedRef` — **חייב להיות מוצהר מעל ה-early returns** (חוקי hooks),
    וה-ref מונע כתיבה כפולה בכל re-render של מסך התוצאה. כשל בכתיבה נרשם ל-console ולא
    עוצר את המסך — הציון כבר נספר ב-`menu_progress`, אז נאבדת רק שורת ההיסטוריה.
    `session.offline` לא כותב כלום.
    ⚠️ **חובה לבחור לפחות מרכיב אחד כדי לשלוח** (הכפתור מושבת אחרת). בלי זה נמצא באג
    בבדיקת עיקריות: `jaccard(∅,∅)=1` (הכלל הנכון "לדעת שאין אלרגיות = ידע"), ולכן שליחה
    **ריקה לגמרי** על מנה בלי אלרגיות (Greek Pomodoro) קיבלה **40%** על כלום. במנה עם
    אלרגיות שליחה ריקה נתנה 0% כמצופה — ההבדל הוא שחשף את הפער.
    📌 הפורמט המקובע ביטל גם את כל שכבת התאמת-הטקסט-בעברית (`normWords`/`variants`/
    `mentions`) — היא **נמחקה**, ואיתה מקור הבאגים של אותיות שימוש. אין יותר NLP בקוד.

  **`pickDistractors(pool, it, count)` — בחירת מסיחים (2026-08-11, משוב משתמש)**: כל
  שאלת multiple-choice (Quiz, Speed, NameCompletion) בוחרת מסיחים **מאותה קטגוריה קודם**
  (`it.category`), ורק אם אין מספיק — משלימה ממנות אחרות. הסיבה: לפני זה המסיחים היו
  רנדומליים לגמרי, אז שאלה על פסטה יכלה לקבל "סלט ירוק" ו"קינוח" כאופציות — פתיר
  באלימינציה בלי לדעת את התפריט בכלל. עכשיו מנת פסטה תקבל פסטות אחרות כמסיחים.

  אתגרים (2026-08-10, עודכן): מחושבים בקוד, לא מטבלת DB ייעודית (אין כזו). כרטיס
  תצוגה מקדימה בבית + טאב "אתגרים" מלא. **תוקן אותו יום**: כרטיסי "שליטה בקטגוריה"
  (אחד לכל קטגוריה) **הוסרו מכאן** — עברו לטאב "תפריט" עצמו (ר' למטה), כי המשתמש
  ביקש שקטגוריות יהיו חלק מהתפריט, לא "אתגר" נפרד:
  - אתגר יומי: 3 מנות חדשות/יום → בונוס חד-פעמי של 50 נקודות (מעקב מקומי,
    `menu-app-daily-<teamMemberId>`, מתאפס כל יום; bonusTotal מצטבר תחת
    `menu-app-bonus-<teamMemberId>`, נכנס לנוסחת הניקוד לצמיתות)
  - **אתגר האלרגנים** — מפעיל AllergenQuiz
  - **התאימו תיאור למנה** — מפעיל NameCompletion (ר' תיאור מעודכן למעלה)
  - שליטה מלאה בתפריט: mastered.size מול cards.length, ללא כפתור פעולה
  - שיא מהירות: `menu-app-best-speed-<teamMemberId>`
  - רצף למידה: `leaderboard.streak`
  ⚠️ **הכל device-scoped** (localStorage) מלבד ה-points הכוללים שמתעדכנים ב-leaderboard.

  טאב "תפריט" (categories, 2026-08-10): **כל קטגוריה עכשיו לחיצה** — לוחצים על
  "ראשונות"/"עיקריות"/"קינוחים" ומופעל Flashcards עם רק הפריטים של אותה קטגוריה
  (`setModeItems(items); setMode("flashcards")`). זה מה שהיה קודם "אתגר שליטה בקטגוריה"
  בטאב אתגרים, עכשיו native לטאב התפריט עצמו.
```

## קבצים מרכזיים (הקוד החי בלבד)

| קובץ | תפקיד |
|---|---|
| `src/main.jsx` | mount של `App.jsx` (תוקן 2026-08-10 — היה `RestaurantDemo`) |
| `src/App.jsx` | ניהול session + ניתוב (כולל שער ה-WelcomeTutorial) |
| `src/auth/TeamLogin.jsx` | מסך הצטרפות (team_code + שם פרטי/משפחה, fuzzy-match) |
| `src/screens/WelcomeTutorial.jsx` | ⭐ חדש 2026-08-11 — 4 שקופיות פתיחה לחבר צוות חדש, כולל תדריך המסעדה שהבעלים כתב |
| `src/screens/MainApp.jsx` | מסך הבית + 5 הטאבים + `learnItem` ⚠️ **המשחקים כבר לא כאן** — ר' `src/games/` |
| `src/games/` | ⭐ 2026-08-13 — משחק לקובץ + `shared.js` (עזרים משותפים). **עזר חדש ⇒ ל-shared.js** |
| `src/lib/supabase.js` | לקוח Supabase (נוצר 2026-08-10 — היה חסר לגמרי!), `persistSession: false` |
| `src/lib/mockMenu.js` | ⚠️ **TEMP DEV FALLBACK** (נוצר 2026-08-10) — עותק סטטי של התפריט האמיתי (19 פריטים) לשימוש כשאין חיבור ל-DB |

## רב-דיירות (multi-tenant) — עדכון 2026-08-10 (עדכון שני)

חלק מתשתית רב-דיירות רחבה יותר (ר' `/Users/homestation/Desktop/CLAUDE.md` לתמונה המלאה).
שני תיקונים ב-**נתיב האמיתי בלבד** (ה-offline fallback לא נגע כלל):

1. **תוקן באג רב-דיירות קריטי**: `MainApp.jsx` שלף `published_menu` בלי שום סינון
   (`db.from("published_menu").select("*")`) — כל צוות היה רואה את המנות של **כל** המסעדות,
   לא רק שלו. תוקן: `.eq("restaurant_id", session?.restaurantId)`.
2. **פרסונליזציה בסיסית**: `TeamLogin.jsx` שולף עכשיו גם `name`/`description`/`cuisine_types`
   של המסעדה (לא רק `id`), ושומר אותם ב-session בתור `restaurantName`/`restaurantDescription`/
   `restaurantCuisineTypes`. `MainApp.jsx` מציג אותם ב-header (ליד שם המשתמש) ובכרטיס קטן
   בראש טאב "בית" — שניהם render מותנה (`session?.restaurantName && ...`) כדי לא לקרוס
   בנתיב ה-offline, ששם השדות האלה לא קיימים.

## חשבונות אמיתיים + התקדמות משוקפת לבעלים — 2026-08-11

ר' תיאור מלא של `TeamLogin.jsx` למעלה. הנקודה החשובה: `team_members` עכשיו כולל
`first_name`/`last_name` (עמודות נוספו במיגרציה, `name` נשאר כטור דנורמל לתאימות עם
`leaderboard.name` הקיים). כל rating שנשמר (`menu_progress`/`leaderboard`) משויך ל-
`team_member_id` האמיתי, ולכן טאב "צוות" באפליקציית הבעלים (`OwnerDashboard.jsx`, ר'
`shiftcrew-owner/CLAUDE.md`) רואה את זה מיד. נבדק חי: יצרתי "בדיקה אחת" דרך ה-waiter app,
הוא הופיע בטאב "צוות" בעל הבית עם 0 נקודות/0 מנות (נכון, כי לא דירג עדיין כלום).

מ-2026-08-11 יש גם commit נוסף (`Eytan Shleizer` — ר' CLAUDE.md הראשי לדיון על הזהות הזו)
שמוסיף דיווח התקדמות-אתגר-יומי + קריאות-brief לשרת (`daily_brief_reads` וכו') — לא נבדק
בפירוט בסשן הזה, אבל מסונכרן ופרוס.

## ⚠️ TEMP DEV FALLBACK — bypass התחברות (Supabase עדיין תקוע)

כל עוד ה-Supabase לא חזר לפעול (ר' CLAUDE.md הראשי, PGRST002), הוספתי מנגנון עוקף כדי
שאפשר יהיה בכלל לראות ולבדוק את האפליקציה:

- **`auth/TeamLogin.jsx`**: אם החיפוש של team_code נכשל (DB לא זמין), במקום להציג שגיאה
  נוצר session מקומי (`{teamMemberId: random uuid, restaurantId: <ה-UUID האמיתי של סלון
  יווני>, offline: true}`) ונשמר ב-localStorage. **כרגע כל קוד/סיסמה עובדים** — זו הייתה
  בקשה מפורשת של המשתמש ("לא משנה מה הסיסמא, בינתיים תכניס אותי") כדי לעקוף את תקלת
  ה-Supabase שלא קשורה לקוד. **יש להסיר את זה כשה-DB חוזר לעבוד** (או לפחות להגביל
  שוב לבדיקה אמיתית מול ה-DB).
- **`App.jsx`**: אם `session.offline === true`, מדלג על אימות מול ה-DB בטעינה מחדש
  (כי ה-team_member לא באמת קיים שם).
- **`screens/MainApp.jsx`**: אם `session.offline === true`, טוען תוכן קבוע מ-`lib/mockMenu.js`
  (אותו תפריט בדיוק שזרוע ב-DB האמיתי) במקום לנסות לשלוף מה-DB, ומדלג על כתיבות
  (menu_progress / leaderboard) — הכל מקומי, שום דבר לא נשמר. יש **באנר צהוב גלוי** בראש
  המסך ("מצב לוקאלי — Supabase לא זמין, כלום לא נשמר באמת") כדי שלא יהיה בלבול עם נתונים
  אמיתיים.
- כל שלושת המקומות מסומנים בקוד עם הערת `TEMP DEV FALLBACK` — חפש את זה כדי למצוא ולהסיר
  בקלות כשה-DB יחזור לפעול.

## ⚠️ מלכודת RTL — קרוסלות/swipers (חשוב לכל דבר עתידי מהסוג הזה)

כל האפליקציה רצה תחת `dir="rtl"`. בניית `PromoCarousel` (2026-08-10) גילתה כמה
התנהגויות RTL לא-אינטואיטיביות ב-Chromium. **תיעוד כדי לא לבזבז שוב שעה על דיבוג**:

1. **`scrollLeft` ב-RTL**: קונטיינר עם `overflow-x: auto` בתוך `dir="rtl"` משתמש ב-
   **ערכים שליליים** ל-`scrollLeft` (לא 0..scrollWidth כמו ב-LTR). קביעת `el.scrollLeft = 100`
   או `el.scrollTo({left: 100})` פשוט לא זזה (מקובעת ל-0, בשקט, בלי שגיאה) — צריך `-100`.
2. **`scroll-snap-type` + `scrollTo({behavior:'smooth'})`**: גם אחרי תיקון הסימן, שילוב
   snap (`mandatory` או `proximity`) עם smooth scroll מתוכנת נוטה "להיתקע" באמצע האנימציה
   (ראינו scrollLeft נעצר על -19px, -63px וכו' במקום להגיע ליעד). לא מצאנו תיקון אמין —
   פשוט **נטשנו native scroll לגמרי** לטובת `transform: translateX()`.
3. **`translateX(<percent>)` על flex track**: האחוז ב-`transform` נפתר יחסית ל**קופסת
   הגבול העצמית של האלמנט המשתנה**, לא לקונטיינר החיצוני. על track ברוחב `500%`
   (5 שקופיות), `translateX(20%)` זז 20% מתוך ה-1980px של ה-track עצמו (=396px, כן
   נכון) — אבל ב**בדיקה בפועל** ב-Chromium headless של הכלי הזה, `getComputedStyle().transform`
   החזיר `matrix(1,0,0,1,0,0)` (זהות) בלי קשר לערך ה-inline — כלומר האחוזים פשוט לא
   *נפתרו/צוירו* בסביבת הבדיקה הזו, מסיבה שלא איתרנו לגמרי. **מסקנה**: להימנע
   מ-`translateX` עם אחוזים על track שרוחבו גם הוא יחסי-אחוזים; אם צריך פיקסלים
   מדויקים — למדוד רוחב בפועל (ResizeObserver) ולהשתמש ב-`translateX(Npx)`. גם זה
   *כן* עבד מבחינת הפתרון (px) אבל ה-state (`slideWidth`) לא התעדכן אמין מ-ResizeObserver
   בסביבה הזו (נשאר 0), אז בסוף **ויתרנו גם על transform-slide וגם על מדידות** —
   הפתרון הסופי שעובד: **רינדור שקופית אחת בכל פעם** (`items[index]`, בלי track/scroll/
   transform בכלל), עם `key={p.id}` + קלאס `animate-fadeIn` קיים (`src/index.css`) כדי
   לקבל fade קליל בכל מעבר. פשוט, אמין, בלי שום בעיית RTL.
4. **סדר ילדים ב-flex row בתוך RTL**: `display:flex` (ברירת מחדל `row`) בתוך `dir="rtl"`
   שם את הילד הראשון **בצד ימין** (לא שמאל כמו ב-LTR) — כל ילד הבא נוסף שמאלה. חשוב
   אם אי פעם עוברים חזרה לגישת track/transform.

**הכלל המעשי**: לקרוסלה/swiper הבא באפליקציה הזו — תתחיל ישר מגישת "שקופית אחת +
fade לפי key", לא מ-scroll או מ-transform-track. זה מה שבסוף עבד.

## הערות חשובות / TODO

- ✅ **תוקן 2026-08-10**: TeamLogin עכשיו מזהה שם חוזר (ILIKE, case-insensitive exact
  match על `restaurant_id + name`) ומשתמש באותו `team_members.id` — לא יוצר כפילות
  ולא מאבד התקדמות. **מגבלה ידועה**: שני אנשים אמיתיים עם אותו שם בדיוק (למשל שני
  "דנה") ישתפו בטעות את אותה שורת התקדמות/ניקוד — לא טופל, ולא התבקש. אם זה קורה
  בפועל, הפתרון הפשוט ביותר הוא לבקש מהם להזין שם משפחה או כינוי ייחודי.
- קטגוריית `drinks` ריקה בתפריט האמיתי — `MainApp.jsx` מסנן קטגוריות ריקות מטאב
  "קטגוריות" כדי לא להציג 0/0.
- אין Supabase Auth (כמו ב-owner) — כל הגישה דרך `anon` key + RLS פתוח.

## הרצה מקומית

```bash
npm install
npm run dev
```
`.env` כבר קיים עם `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`.

## סטטוס Git (2026-08-11)

✅ **הכל committed ו-pushed** — `origin/main` מסונכרן, כולל השינויים שתוארו למעלה (חשבונות
אמיתיים, fuzzy match) וגם commit נוסף של Eytan Shleizer מעל זה. `git status` אמור להראות
עץ נקי; אם לא — יש עבודה לא-שמורה משיחה קודמת, תבדוק לפני שממשיכים.

ה-TEMP DEV FALLBACK (offline session ב-`TeamLogin.jsx`/`App.jsx`/`mockMenu.js`) **עדיין
קיים בקוד ופרוס ל-production** — Supabase כבר לא תקוע, אז זה בפועל כמעט אף פעם לא מופעל
(רק אם ה-DB זמנית לא זמין), אבל לא הוסר. זו עדיין החלטה פתוחה של המשתמש אם להסיר או
להשאיר כ-graceful degradation — לא הוסר יזום בלי לשאול.

## 🔤 לקסיקון האלרגיות שונה + הדגשת השאלה במשחקים (2026-08-16, החלטת משתמש)

**החלטת מוצר של המשתמש** (מתועדת כהחלטתו; רפואית אלרגיית דגים ואלרגיית חלבון־חלב
קיימות — הוצג לו והוא הכריע): במערכת הזו "דגים" **אינו** ערך אלרגיה (דג נא שייך
ל-pregnancy), ו"חלב" הוחלף ב-**"לקטוז"**. הרשימה עכשיו **8 ערכים**:
גלוטן, לקטוז, ביצים, אגוזים, בוטנים, רכיכות, סויה, שומשום.

- ⚠️ הרשימה משוכפלת: `games/shared.js`, `lib/dishFlags.js`, שלושה קבצים ב-owner,
  ו-`FLAG_VALUES` ב-Edge Function (v24 פרוס). **שינוי ⇒ בכולם.**
- מיגרציית נתונים רצה: 48 מנות חלב⇒לקטוז, 24 מנות איבדו 'דגים', 0 מונחים ישנים
  נשארו ב-`menu_items`/`published_menu` (421 מנות).
- **הדגשת השאלה**: ה-prompt בכל המשחקים (חידון/מבחן/תיאור/מהירות/אלרגיות) היה
  11px אפור מול שם מנה 18px לבן — השאלה נעלמה. עכשיו: שאלה 15px לבן font-black,
  שם המנה בסגול `#a79bff`. השאלה היא הכותרת; שם המנה הוא הפרמטר שלה.

## מוקשים + תיקוני קפיצות (2026-08-13)

- **מוקשים (`pitfalls`)** — שדה נפרד מ-`allergens`. `דג נא` הוא מוקש, לא אלרגן.
  כרטיסייה: ספירה לפני החשיפה, פירוט אחריה (צהוב, נפרד מהאדום של האלרגיות).
  facet `pitfalls` + `qPitfallDish` ב-`questionEngine.js`.
  ⚠️ `qPitfallDish` מחזיר `null` כשרוב התפריט נושא את אותו מוקש — זה מה שמונע
  חזרה על "כל המנות מכילות דגים". שורה 15 ב-`QUESTION-QUALITY.md`.
- **`gameItems` מוקפא לכל אורך הסבב** (`useMemo` על `[mode, modeItems]` בלבד).
  `path.gamePool` נגזר מ-`masteryById` ולכן מייצר מערך חדש **בכל תשובה** — בלי
  ההקפאה כל דק של כל משחק נבנה מחדש באמצע סבב. זה היה השורש גם של "החידון מדלג"
  וגם של "ההתאמה קופצת ללוח אחר".
- **חידון**: 1800ms משוב נעול לפני מעבר, בלי הזזת אופציות. **התאמה**: הלוח נשאר עד
  שמסיימים אותו, ואחרי 2 לוחות מסך תוצאה. השארת התאמות שגויות על הלוח היא בכוונה.
- `countLabel` — ריבוי בעברית ("1 מוקש", לא "1 מוקשים").

## מדדים, קנס זמן, ושאלות דו-משמעיות (2026-08-13)

- **`MetricsScreen.jsx`** (חדש) — "המדדים שלי", כפתור 📊 בראש המסך. הכל **נגזר** מנתונים
  קיימים (`menu_progress`, `total_seconds`, `progress_snapshots`, `leaderboard.streak`);
  אין רישום חדש. שיא הרצף נגזר מימי הסנאפשוטים — בטבלה נשמר רק הרצף הנוכחי.
- **התאמה**: `WRONG_MATCH_PENALTY_S = 5`. הזמן כולל קנסות נשמר ל-`menu_app.match_times`,
  ובסוף מוצגת טבלת 3 השיאים של המסעדה. ⚠️ ההשוואה מסננת לפי `boards`.
- ⚠️ **שאלה עם יותר מתשובה נכונה אחת** — ר' שורה 16 ב-`QUESTION-QUALITY.md`.
  השערים מודדים **דמיון חזותי**, לא אמת. בדיקת `AMBIGUOUS` גוזרת את האמת מחדש
  ולא סומכת על ה-builder. **אל תשנו builder בלי להריץ אותה.**
- **שאלון אלרגיות**: פסק דין מפורש ✓/✗ + שלושה מצבים לצ'יפ (סימנתם ונכון / פספסתם /
  סימנתם בטעות). "אין אלרגיות" שגוי היה נראה כמו תשובה נכונה.

## בריף לפני משמרת + מנות חדשות (2026-08-13)

`src/components/ShiftBrief.jsx` — ראשון במסך הבית, **תמיד**, גם ביום בלי עדכונים
("אין עדכונים חדשים · הכל כרגיל"). בריף שמופיע רק כשיש בו תוכן הוא **התראה, לא
בריף** — והמטרה כאן היא לדמות את מסירת המשמרת של המנהל כהרגל יומי.

- **סדר**: הבריף למעלה (קריטי לזמן — מה חסר הערב), המנות החדשות מתחתיו (שיעורי בית).
- **עיצוב**: שניהם דרך קומפוננטת `Panel` אחת — אותו משטח, פינה, וכותרת עם פס-צבע
  ואייקון. הם אמורים להיקרא כשני חלקים של אותה מסירה, לא כשני ווידג'טים.
- **מנות חדשות**: `NEW_DISH_WINDOW_DAYS = 21` **וגם** `mastery === 0`. מקובץ לפי
  קטגוריה — 3+ מנות נקראות כתפריט שלם ("כבר למדת את תפריט X החדש?"), אחרת כמנה.
  הכפתור מזריק `setModeItems(newDishes)` ⇒ כרטיסיות של אותן מנות בלבד.
  ⚠️ חלון הזמן מונע ממלצר שפשוט לא למד לראות את כל התפריט כ"חדש" — הבקלוג הזה
  הוא תפקיד מסלול הלמידה.
- `last_seen_at` נכתב בכל טעינה (fire-and-forget) — מזין את טאב הסטטוס של הבעלים.

## משחק המהירות

`SPEED_EXIT_AFTER_S = 10` — יציאה נפתחת אחרי 10 שניות. קודם לא הייתה יציאה בכלל
(טעות עלתה 30 שניות); יציאה מיידית הייתה הופכת אותה להגרלה מחדש של הדק.

## ⚠️ שני כשלים שה-build לא תפס (2026-08-13)

שניהם עברו `vite build` בהצלחה ונתפסו **רק בדפדפן**:
1. **`countLabel` הוגדר בתוך הקומפוננטה** (מעל `scorePct`, שהוא מקומי ל-`MainApp`)
   ולכן `Flashcards` — קומפוננטה אחות — לא ראה אותו. `ReferenceError` בזמן ריצה.
2. **`useMemo` אחרי early return** ⇒ "Rendered fewer hooks than expected".
   **כל ה-hooks מעל כל ה-`if (mode === ...) return`.**

**המסקנה: build ירוק הוא לא אימות.** הריצו את המסך בפועל.

## ⚠️ `pregnancy` / `kashrut` — צנרת מתה כרגע

`pubToCard` קורא אותן, אבל **הכרטיסייה לא מציגה אותן ואין עליהן שום שאלה** (אין
facet ב-`questionEngine.js`), ו-`src/lib/dishFlags.js` מגדיר את ארבע הקבוצות אבל
**לא מיובא לשום מקום**. בנתונים: `pregnancy` ו-`kashrut` ריקים ב-94/94 המנות,
ו-`דג נא` עדיין יושב ב-`pitfalls` (56 מנות). ר' CLAUDE.md הראשי, "מצב בפועל בנתונים".

## הקרוסלה + אישור קריאת הבריף (2026-08-13)

**`PromoCarousel`** — **בדיוק שלושה שקפים**: עדכון יומי ⇒ מנות חדשות ללמידה ⇒
סבב הלימוד הבא. ⚠️ שקפים 2 ו-3 **תמיד מרנדרים כרטיס**, אחרת הקרוסלה יורדת לשניים:
שקף 2 נופל למנות שעדיין לא 5/5 ואז לתרגול חוזר; שקף 3 נופל ל"סבב לימוד מותאם
אליכם" כשכל הקטגוריות כבר עברו. `ShiftBrief.jsx` נמחק (שכפל את שקפים 1–2).
שקפי האתגר היומי/הדירוג/המשחקים הוסרו — לכולם יש מקום משלהם במסך הבית ובטאבים.

**אין עדכון יומי ⇒ הפניה ללמידה (2026-08-13)**: ביום בלי בריף, שקף "עדכון יומי"
אומר "אין כרגע עדכון יומי... בואו נלמד" וההקשה פותחת את סבב ה-quick — לא את הטאב
הריק. גם טאב יומי ריק מציג את אותה הודעה + כפתור ללמידה. כשיש בריף — ללא שינוי.
⚠️ הקשה שמובילה למסך ריק היא מבוי סתום; כל מצב-ריק באפליקציה צריך הפניה הלאה.

**`BriefAck.jsx`** (טאב "יומי") — ⚠️ קריאת הבריף נרשמה קודם **אוטומטית בטעינה**,
כלומר מדדה "פתח את האפליקציה" ולא "קרא". עכשיו: שאלה אחת שנבנית מתוכן הבריף,
עם מסיחים שהם מנות אמיתיות **שלא מופיעות באף רשימה של הבריף** — אחרת יש שתי
תשובות נכונות. בריף עם הערות בלבד ⇒ אישור פשוט; **לא ממציאים שאלה**.

**"לימוד האלרגיות"** (לא "אתגר") — אלרגיה יכולה לשלוח סועד לבית חולים; מסגור
כמשחק מוריד מהרצינות. שאר המשחקים נשארו "אתגר".

## סבב לימוד מבוסס ביצועים (2026-08-13)

`src/lib/studySession.js` — **כניסה לקטגוריה כבר לא פותחת את כל המנות שבה.**
`buildStudySession(items, progressById)` מחזיר `{ deck, retiredCount, poolCount, allRetired }`:
מקסימום 10 מנות שונות, חלשות קודם, מנה שדורגה ≤2 מקבלת סיבוב שני **עם מרווח**,
ומנה שקיבלה 5 **פעמיים ברצף** פורשת מהרוטציה.

- ⚠️ נדרשה עמודה חדשה `menu_progress.consecutive_fives`: `mastery` הוא הדירוג
  **האחרון בלבד**, ולכן לא יכול לבטא "הצליח פעמיים ברצף". 5 מעלה, כל דירוג נמוך מאפס.
- ⚠️ **מנה שלא נראתה לא מקבלת חזרה כפולה** — אי אפשר להיכשל במנה שעוד לא ענית עליה.
  היא כן בעדיפות הגבוהה ביותר להיכנס לסבב (`priority(null) = 7`).
- ⚠️ `studySession` **מוקפא לכל אורך הסבב** (deps `[gameItems]` בלבד) מאותה סיבה
  כמו `gameItems` — דירוג משנה את מפת ההתקדמות, ובנייה מחדש באמצע הייתה מערבבת
  את הדק תחת הידיים.
- מסך הסיום אומר כמה מנות דילגנו עליהן וכמה נשארו — "סיימת" לבדו נקרא כאילו
  סיימת את כל הקטגוריה, וזה בדרך כלל לא נכון.

```bash
node tests/session.test.mjs
```

## 🔒 נקודות רק על ידע שנבדק (2026-08-13)

`learnItem(id, rating, { objective })` — **`objective: false` רק מכרטיסיות.**
כרטיסיות הן דיווח עצמי, ועד לשינוי הזה הן קראו לאותו `learnItem` כמו המשחקים —
כלומר לחיצה על "5" לאורך כל הדק נתנה את מלוא הנקודות בלי שום בדיקה.

- `menu_progress.verified` נדלקת **רק** ב-4+ ממצב שמנקד את עצמו (חידון/התאמה/
  מהירות/אלרגיות/תיאור/מבחן). `mastered` = `mastery >= 4 && verified`, וממנו הנקודות.
- תשובה שגויה במצב נבדק מורידה מתחת ל-4 **ומכבה את האימות** — הנקודות יורדות איתה.
- דירוג עצמי עדיין מזין את `studySession` ואת אחוז ההתקדמות. יש משפט במסך
  ("הדירוג העצמי קובע מה תחזרו עליו — נקודות נצברות במשחקים ובמבחנים") כדי שהמלצר
  לא ידרג 5 ויתהה למה אין נקודות.
- `daily.ids` — האתגר היומי סופר כל מנה פעם אחת ביום (מניעת הורדה+העלאה חוזרת).

⚠️ **אל תעבירו `objective: true` מכרטיסיות** — זה מחזיר את הפרצה במלואה.

## 🔓 מסלול פתוח (2026-08-13) — שכתוב של `learningPath.js`

**אין יותר שרשרת ואין נעילות.** `unlocked: true` לכל קטגוריה, וכל מצבי התרגול
פתוחים מההתחלה (`GAME_MODES`, בלי `afterPassed`).

**מה שמבחן משנה זה `gamePool`, לא גישה**:
- עברת ראשונות ⇒ כל התרגול ראשונות. עברת ראשונות+קינוחים ⇒ מעורבב.
- קטגוריה שלא נפתחה **אף פעם לא דולפת** לתרגול.
- לפני המבחן הראשון: נפילה ל-`recommended` — עדיין נושא אחד, לא כל התפריט.
- `recommended` = הראשונה שלא עברת, **הכוונה בלבד**; `null` כשהכל עבר.

`path.openKeys` = הקטגוריות שהתרגול נבנה מהן כרגע. ה-UI אומר את זה בקול
("התרגול כולל: מאקי · סשימי") — אחרת "למה אני מקבל רק סשימי?" הוא שאלה בלי תשובה.

⚠️ **היקף צר ⇒ מצב שלא ניתן לבנות** (סשימי = 4 מנות במרכיב אחד ⇒ אין חידון).
**אל תרככו את שערי האיכות בשביל זה** — `NotEnoughData` מסביר את ההיקף ומה מרחיב אותו.

```bash
node tests/path.test.mjs   # כתוב מחדש סביב המפרט הזה
```

## אופטימיזציית צד המלצר (2026-08-13) — 5 פיצ'רים ממסמך ההחלטות

### ⚡ "5 דקות לפני משמרת" (`mode: "quick"`)

כפתור ראשון במסך הבית. `buildStudySession(cards, progress, QUICK_SESSION_SIZE, rnd,
{ repeatWeak: false })`.

- **אותו אלגוריתם, גודל קטן** — לא כלל בחירה שני. `buildStudySession` כבר מדרג
  לא-נראתה (7) > חלש > מסומן (`STAR_BOOST`), כלומר "מה שסומן + מה שחדש + מה שהכי
  חלש" הוא בדיוק הפלט שלו. כלל נפרד היה סוטה ממנו עם הזמן.
- ⚠️ **`repeatWeak: false` חיוני**: הסבב הרגיל מוסיף סיבוב שני למנות ≤2, וזה הפך
  5 כרטיסים ל-**8** והפך את הכיתוב "5 מנות" לשקר. יש בדיקה על שני הכיוונים.
- ⚠️ **מושך מכל התפריט, לא מ-`path.gamePool`** — מנה שהמנהל דוחף היום חשובה בלי קשר
  לאיזה מבחן עברת. זה כרטיסיות (דיווח עצמי), אז אי אפשר לצבור ממנו נקודות בכל מקרה.

### ⭐ כוכב המנהל (`starred || is_special`)

- **שתי עמודות, כוכב אחד**: הטוגל של המנהל (owner app, 2026-08-13) כותב לעמודה
  `starred`; `is_special` היא הדגל הישן שמנות זרועות עדיין נושאות. `pubToCard` קורא
  **את שתיהן** (`isSpecial: !!(p.starred || p.is_special)`).
  🔴 **הבאג שתוקן**: צד המלצר קרא רק את `is_special`, כלומר המנהל לחץ ⭐ וכלום לא
  קרה אצל הצוות. שני סבבים מקבילים בחרו עמודות שונות — כשמוסיפים דגל שחוצה אפליקציות,
  בדקו איזו עמודה **הצד השני** קורא לפני שממציאים חדשה.
- כרטיסייה: כוכב ליד השם **ומשפט** — "המנהל סימן: זו מנה חשובה ונמכרת". הכוכב לבדו
  קישוט; המלצר לא יודע למה המנה שונה. **המשפט הוא הפיצ'ר.**
- `STAR_BOOST = 2` ב-`priority(entry, item)`. ⚠️ **קידום, לא עקיפה** — מנה מסומנת
  שכבר 5/5 עדיין נופלת מאחורי מנה שלא נראתה, אחרת אותן מנות בראש כל סבב לנצח.
- ℹ️ בצד המנהל יש גם **auto-star** למנה בודדת שנוספה אחרי 48h שקט — היא תקבל עדיפות
  אצל המלצרים אוטומטית דרך אותו מסלול. מכוון.

### ⏱️ יעד יומי בדקות — המנהל קובע

`exam_config.daily_goal_minutes` (ברירת מחדל 10, `CHECK 1..120`). טבעת במסך הבית.

- `useStudyTime({ onSecond })` מדווח כל שנייה — בלי זה הטבעת קופצת רק כל פליחה (2 דק').
- `todaySeconds` נזרע מ-`progress_snapshots` של היום בטעינה, כדי שרענון לא יאפס.
- ✅ **מסך ההגדרה בצד המנהל נבנה** (2026-08-14): טאב "הגדרות" ⇒ "יעד לימוד יומי",
  5/10/15/20 דקות. נבדק מקצה לקצה: 15 בצד המנהל ⇒ "0/15 דקות מהיעד היומי" בבית.

### 🏆 ניקוד שבועי (`weekly_scores` + `add_weekly_points`)

⚠️ **הניקוד השבועי נצבר, לא נגזר.** הניקוד הכולל הוא פונקציה טהורה של כמה מנות
מאומתות **עכשיו**; "כמה צברת השבוע" הוא סכום אירועים שאי אפשר לשחזר מהמצב הנוכחי.
לכן טבלה נפרדת ו-RPC אטומי (`add_weekly_points`) ולא read-modify-write.

- +100 כשמנה חוצה למאומתת, **-100 כשהיא נופלת החוצה** (סימטרי, כמו הניקוד הכולל).
  בונוס יומי (+50) גם הוא נכנס לשבוע שבו הושג.
- שבוע מתחיל **ביום ראשון**, ב-SQL וב-JS. ⚠️ אם צד אחד ישתנה — ניקוד ייכתב לשבוע
  אחד וייקרא משבוע אחר.
- טאב "דירוג": השבוע (ברירת מחדל) / כל הזמנים. `myRank` בכותרת עוקב אחרי השבועי,
  ונופל לכולל לפני שנצברו נקודות השבוע.

### ↩️ המשך מאיפה שהפסקת

`menu-app-resume-<memberId>` ב-localStorage, עם `at` ו-`RESUME_MAX_AGE_MS = 12h`.

- ⚠️ **`exitMode` מוחק את הרשומה** — יציאה מכוונת אינה "נקטע". רק סגירת האפליקציה
  באמצע סבב שווה הצעה.
- שומר `categoryKey` כדי שסבב מקוטלג יחזור לאותו היקף ולא לדק חדש של כל התפריט.

### ⚠️ שלישי באותו יום: build ירוק שהסתיר `ReferenceError`

`Repeat` (אייקון) שימש בכרטיס ההמשך אבל לא היה ב-import — `vite build` עבר, הדפדפן
קרס. **JSX עם רכיב לא-מיובא אינו שגיאת build.** בדיקה מהירה לפני commit:

```bash
python3 - <<'PY'
import re
s = open("src/screens/MainApp.jsx", encoding="utf-8").read()
imported = set(x.strip() for x in re.search(r'import \{([^}]*)\} from "lucide-react";', s).group(1).split(","))
used = set(re.findall(r'<([A-Z][A-Za-z0-9]*)\s+size=', s))
local = set(re.findall(r'function ([A-Z][A-Za-z0-9]*)\s*\(', s))
print("missing:", sorted(used - imported - local - {"Icon"}))
PY
```

## חבילת כיף/פשטות/נגישות (2026-08-13, לילה) — מבנה חדש

### 📁 `src/games/` — המשחקים חולצו מ-MainApp.jsx

MainApp ירד מ-2151 ל-~1150 שורות. קובץ לכל משחק + `shared.js` לעזרים המשותפים
(catLabel/shortCat/countLabel/shuffle/colorFor/ALLERGENS/PITFALLS/FEEDBACK_MS/
todayStr/loadDaily/saveDaily/loadNum/saveNum). **MainApp מייבא מהם** — אין יותר
עותקים מקומיים. הסיבה: שלוש קריסות ריצה ביום אחד מעזרים שהוגדרו בהיקף לא נכון.
⚠️ עזר חדש למשחקים ⇒ ל-`shared.js`, לא לתוך MainApp.

### 🎴 היפוך כרטיס (`Flashcards.jsx` + CSS ב-`index.css`)

`.flip-scene`/`.flip-card`/`.flip-face` — **rotateX ולא rotateY**: היפוך אופקי
"נפתח הפוך" ב-RTL. הכרטיס ממופתח `key={it.id}` כדי שמעבר לכרטיס הבא ירונדר
לא-הפוך בלי אנימציית היפוך-חוזר. כל הפאה הקדמית היא כפתור.

### 🔥 `StreakToast.jsx`

מ-3 נכונות ברצף, בתוך-סבב, בחידון/מהירות/אלרגיות. `key={streak}` מנגן את
האנימציה מחדש בכל אבן דרך; ה-CSS מסתיים ב-opacity 0 עם `forwards` — אין טיימרים.
לא קשור לרצף הימים.

### 🏠 מסך הבית — ארבעה בלוקים

המשך (אם יש) ⇒ **כרטיס-על** (טבעת יעד + "5 דקות לפני משמרת", הקשה אחת) ⇒ קרוסלה
⇒ תרגול (פס התקדמות + כל 6 המצבים). מה שהוסר ולמה — בהודעת ה-commit `235ec49`.
⚠️ כרטיס "אתגר יומי" הישן ספר 3 מנות — מדד שהוחלף ביעד הדקות. אל תחזירו אותו.

### 🏅 טאב אתגרים = רק מצבים לנצח

יעד יומי, שליטה מלאה, שיא מהירות, רצף. משגרי משחקים לא חוזרים לכאן — הם בגריד
התרגול. "התאמת תיאור" **בגריד** עכשיו (הסרת הסינון שלה הייתה תנאי להסרה מהאתגרים).

### ♿ רצפת טיפוגרפיה

במשחקים ובתוויות אינטראקטיביות: מינימום 11px, מטרות הקשה ראשיות 44px.
מסכים צפופים (מדדים) נשארו כמו שהם בכוונה.

## 📱 2026-08-14 — Capacitor + session tokens (ר' CLAUDE.md הראשי לתמונה המלאה)

- `capacitor.config.json` + `ios/` + `android/` (appId `il.co.shiftcrew.team`).
  אחרי כל `npm run build` ⇒ `npx cap sync`.
- `index.html`: נוסף `viewport-fit=cover` — בלעדיו ה-safe-area שכבר היה בקוד
  (כולל ה-bottom nav) חושב ל-0. ה-header של MainApp קיבל inset עליון.
- **`TeamLogin.jsx` שוכתב על `team_join` (RPC)**: כל הזרימה — חיפוש מסעדה, exact
  match, fuzzy (Levenshtein≤2 ⇒ status:'confirm'), יצירת חבר צוות — רצה בשרת,
  שגם מנפיק את הטוקן. ה-levenshtein המקומי נמחק. ה-offline fallback נשאר.
- **טוקן**: `lib/appSession.js` + fetch מוזרק ב-`lib/supabase.js` (header
  `x-app-session`). sign-out מנקה גם את הטוקן (App.jsx).
- **`DeleteProfile` ב-`MetricsScreen.jsx`** — מחיקת פרופיל עצמית
  (`delete_my_team_profile`, cascade). דרישת חנויות. מוסתר ב-offline.

## 🔄 2026-08-15 (ערב) — סנכרון בין המכונות הושלם + רי-ברנדינג CrewMenu

- המק החדש משך את `ad9f877` (אייקוני המותג, טורקיז) מ-origin/main. קונפליקט יחיד
  ב-`index.html` נפתר: favicon החדש + title החדש ("CrewMenu — צוות") נשמרו יחד.
- **רי-ברנדינג**: appId → `il.co.crewmenu.team`, שם תצוגה "CrewMenu צוות". השם
  הקובע מעתה: **CrewMenu**.
- **Haptics** (`@capacitor/haptics`): `src/lib/haptics.js` + קריאה ב-`learnItem`
  (MainApp.jsx) — רטט הצלחה/שגיאה בכל המשחקים המדורגים, לא בכרטיסיות. no-op ב-web.
- `Info.plist`: נוסף `ITSAppUsesNonExemptEncryption=false`.

## 🍏 2026-08-17 — iOS build 6 הועלה ל-App Store Connect מ-homestation

הועלה יחד עם אפליקציית הבעלים, באותו צינור. **התיעוד המלא של צינור ה-iOS
(פקודות, ExportOptions, אימות, העלאת מספר build) נמצא ב-`shiftcrew-owner/CLAUDE.md`,
סעיף "iOS builds הועלו ל-App Store Connect"** — לא משוכפל כאן. נכון לעכשיו:
גרסה 1.0, build **6**, teamID `Q9994667T8`.

## 📋 2026-08-19 — עדכון יומי של המסעדה (שער) + תרגול לפי קטגוריה ומנה

שני הפיצ'רים מהדמו המאושר (artifact `da955a27`), מומשו ונבדקו חי מול CREWDEMO.
**כל השאר לא השתנה** (בקשת משתמש מפורשת) — 5 טאבים, כרטיסייה, עיצוב.

**1. שער העדכון היומי — `src/components/BriefGate.jsx`**
- בכניסה הראשונה של היום (אין שורת `daily_brief_reads` להיום) הבריף משתלט על המסך:
  קריאה ⇒ עד 3 שאלות מתוכנו (חסר/ממליצים/מלאי מוגבל, אותם כללי מסיחים כמו BriefAck —
  מנות אמיתיות שלא מופיעות באף רשימה). טעות ⇒ חזרה לטקסט + נעילת **15 שניות**.
- **3 גרסאות (א/ב/ג)** לסדר השאלות והתשובות, לפי `hash(member+date) % 3` — יציב
  ליום, שונה בין מלצרים. הגרסה מוצגת בכותרת השאלות.
- מעבר כותב את שורת ה-ack הרגילה (`daily_brief_reads`) — לוח הקריאה של המנהל
  ממשיך לעבוד בלי שינוי. בריף של הערות בלבד ⇒ אישור פשוט בלי שאלות.
- **כפתור "לעבור שוב"** בטאב יומי ⇒ אותו שער במצב תרגול (`practice`) — לא נוגע ב-ack.
- לא מוצג ב-offline, לא בלי בריף להיום, ולא לפני שהתפריט נטען (המסיחים ממנו).
- ⚠️ שגיאת כתיבה של ה-ack **לא חוסמת את הכניסה** — השער לעולם לא מקרקע את האפליקציה.

**2. טאב תפריט — דריל-דאון + תרגול פרוגרסיבי**
- לחיצה על קטגוריה ⇒ מסך "לתרגול X:" עם המנות אחת-אחת (`catView` state), כפתור
  "תרגול X", פס התקדמות פר-מנה ו-"✓ הובנה" למנות שפרשו. לחיצה על מנה ⇒ היא
  הכרטיס הראשון של סשן רציף.
- **`src/lib/progressiveSession.js`** — "חלון מתקדם": פעילות רק 4 הראשונות בסדר
  התפריט שטרם הובנו (`consecutive_fives < 2`); הובנה ⇒ הבאה נכנסת; משקל לפי דירוג
  אחרון (לא-נראתה=7, ≤2 מוכפל); בלי חזרה עצמית; 15% רענון מהמובנות; הכל הובן ⇒
  סבבי רענון. `node tests/progressive.test.mjs`.
- **`src/games/ProgressiveFlashcards.jsx`** — הכרטיסייה הרגילה (עותק ויזואלי מלא של
  Flashcards) אבל דירוג ממשיך אוטומטית למנה הבאה. מדרג דרך `learnItem(...,
  {objective:false})` — אותם כללי נקודות. progRef מקומי כי ה-state של ההורה אסינכרוני
  והבחירה הבאה חייבת לראות את הדירוג שزה עתה ניתן.
- progressive **מוחרג מ-resume** (אין דק קבוע לחזור אליו) ומעדכן `menu_progress`
  כרגיל — הובנה נשמרת בין sessions (נבדק: reload ⇒ "✓ הובנה" נטען מה-DB).

⚠️ ה-3-tab reduction הלא-committed הופרש ל-`git stash` ("3-tab nav reduction") —
המשתמש ביקש שהכל יישאר כמו האפליקציה החיה (5 טאבים). ההחלטה עליו פתוחה.

## 📖 2026-08-20 — טעות = רגע לימוד (`src/games/DishReveal.jsx`)

בקשת משתמש: "המטרה שבאמת ילמדו את התפריט." אחרי תשובה שגויה בכל משחק מדורג —
חידון, מהירות, אלרגיות, התאמת תיאור — מופיע **כרטיס המנה המלא** (תיאור, מרכיבים,
אלרגיות, מוקשים, באותו עיצוב כמו גב הכרטיסייה): ספירה לאחור של `REVEAL_SECONDS = 8`
עם התקדמות אוטומטית, וכפתור "הבנתי, ממשיכים ←" לדילוג מיידי.

- **התאמה מוחרגת** (בקשת המשתמש; גם טכנית — לוח לא נעצר פר-זוג), וכן **המבחנים**
  (CategoryExam/QuizExam) — מבחן מודד, לא מלמד באמצע.
- **מהירות: השעון מושהה** בזמן שהכרטיס מוצג (`reveal` ב-deps של אפקט הטיימר) —
  הלימוד לא אוכל את 30 השניות.
- באלרגיות הכרטיס מופיע **אחרי** מסך פסק הדין (✗ + מה פספסתם), לא במקומו.
- הניסוח "הובנו/הובנה" הוחלף בכל האפליקציה ל"הצלחתם להכיר / מכירים" (בקשת משתמש,
  אותו יום).
- ה-reveal הוא `if (reveal) return <DishReveal/>` אחרי כל ה-hooks — לא לשבור את
  כלל ה-hooks-מעל-early-returns.

נבדק חי: טעות בחידון ⇒ הכרטיס עולה אחרי המשוב האדום ⇒ נספר לאחור ⇒ ממשיך לבד
לשאלה הבאה. `npm run build` + כל ארבע חבילות הבדיקות ירוקות.

## ⚡ 2026-08-20 — בוחן זריז לקטגוריות רזות (`src/games/MenuCheckQuiz.jsx`)

בקשת משתמש: לשתייה קלה אין מרכיבים/תיאורים ⇒ אף שאלה רגילה לא נבנית שם, אבל מה
שמלצר באמת צריך לדעת זה **מה מגישים**: יש קולה, אין פפסי. בדף קטגוריה "רזה"
(≥50% מהפריטים בלי מרכיבים ובלי תיאור, ≥3 פריטים) מופיע כפתור
"⚡ בוחן זריז — מה יש אצלנו?" (mode `menucheck`): 8 שאלות בשתי צורות מתחלפות —
"איזה משקה יש אצלנו?" (אמיתי מול 3 מסיחי מותגים) ו"איזה לא נמצא אצלנו?"
(מסיח מול 3 אמיתיים). נכון→5, שגוי→2, אותו `learnItem`.

⚠️ **כלל הבטיחות (לקח ה-AMBIGUOUS)**: מסיח מרשימת `DECOYS` כשיר רק אם אף מילה
שלו לא מופיעה בשום שם פריט **בכל התפריט** (לא רק בקטגוריה) — אחרת "קולה זירו"
כמסיח מול "קולה / קולה זירו" היה שתי תשובות נכונות. נבדק חי על סטודיו 2026:
צורה א' (קולה מול פפסי מקס/פיוז-טי/סבן אפ) וצורה ב' (קינלי מול אמיתיים).

## 🥤 2026-08-20 — קטגוריות רזות: כרטיסי קבוצה + מבחן בתוך הדף (טרם committed)

שלושה שינויים מבקשות המשתמש, כולם נבדקו חי:
1. **`GroupFlashcards.jsx`** (mode `groupcards`): בקטגוריה רזה, "תרגול X" פותח
   כרטיסי **קבוצה** — חזית "שתייה קלה מוגזת", גב = רשימת המשקאות **עם מחירים**
   (כרטיס פר-פריט שם היה קולה⇒קולה — לא מלמד כלום; כלל "בלי מחירים במשחקים"
   נגע לידע מנות, לא לרשימת אספקה). הקיבוץ הוא סיווג מילות-מפתח תצוגתי
   (חמה/מוגזת/קרה לא מוגזת, "תה קר" נבדק לפני "תה"); מה שלא סווג ⇒ "עוד",
   לעולם לא נזרק. דירוג 1-5 אחד חל על כל חברי הקבוצה (self-report).
2. **כפתור מבחן בתוך דף הקטגוריה** — אותו שער ואותה הפעלה כמו הכפתור ברשימת
   הקטגוריות (`cat.examUnlocked`); נעול מציג "הגיעו ל-X% כדי להיבחן".
3. **שאלות בלי שלילה כפולה** — "אורח מבקש ללא X. איזו אסורה?" ⇒
   `איזו מנה מכילה X?` / `באיזו מנה יש X?`. שורה 17 ב-`QUESTION-QUALITY.md`;
   הבדיקה נתלית עכשיו ב-`subjectKind` (לא בטקסט הפרומפט) + שער שסופר שלילות.

ℹ️ בנוסף ב-DB של סטודיו 2026: 63 פריטי אלכוהול/יין קיבלו תעתיק לטיני בשם
("מקאלן 12 - Macallan 12") כדי שמלצרים ידעו לבטא — רק היכן שהמותג ודאי.

## 🧭 2026-08-20 — פישוט הבית והניווט (סבב חדש, טרם committed)

שישה שינויים מרשימת המשתמש (הדמו אושר/ממתין לאישור בסשן):
1. **כרטיס "5 דקות לפני משמרת" הוסתר מהבית** — זמן הלימוד הוא מדד של המנהל, לא
   של המלצר. `mode: "quick"` עצמו קיים ונגיש מהקרוסלה (כשאין בריף) ומטאב יומי ריק.
   המדידה (`useStudyTime`, snapshots, `total_seconds`) ממשיכה לרוץ ללא שינוי.
2. **הקרוסלה = שקף אחד בלבד: העדכון היומי.** שלושה מצבים: בריף שלא נקרא (כתום,
   "לעדכון המלא") / נקרא (ירוק, "קראתם כבר את העדכון היומי") / אין בריף (הפניה
   ללמידה). שקפי "מנות חדשות" ו"השלב הבא" הוסרו.
3. **טאב "אתגרים" הוסר** (הדף בלבד) — המשחקים נשארו בגריד התרגול בבית. מערך
   ה-challenges נמחק כולו מ-MainApp.
4. **שער הבריף (BriefGate) + תרגול פרוגרסיבי** — נכנסו סופית (היו בעץ העבודה
   מסשן הפישוט).
5. **טאב "דירוג" הוסר; הדירוג עבר למסך המדדים (📊)** — `Board` ב-`MetricsScreen.jsx`:
   כותרת "אתם במקום X!", טוגל שבוע/כל הזמנים, אותם כללים. MainApp מעביר
   `weekly`+`leaderboard` כ-props (ה-realtime subscription נשאר ב-MainApp).
   **גרפי הדקות/נקודות הוסרו** מהמסך (וגם stat "זמן לימוד") — זמן = מדד מנהל.
6. **הניווט: 3 טאבים** — בית · יומי · תפריט. `WelcomeTutorial` עודכן בהתאם.

ה-stash "3-tab nav reduction" לא הוחל בפועל (נחסם על קבצים ששונו) — השינויים
יושמו ידנית מעליו; אפשר למחוק אותו אחרי ה-commit.

### 🎨 2026-08-20 (המשך) — מסך הבית עוצב מחדש לפי סקיצת המשתמש

הסקיצה (Excalidraw): בועת פתיחה אישית ⇒ שני כרטיסים זה-לצד-זה ⇒ בלוק התרגול.
**הצבעים לא שונו** (דרישה מפורשת) — רק הפריסה:
1. **בועת פתיחה** — "שלום {שם פרטי} 👋" + "השבוע למדת X דקות 💪 · מקום Y בצוות"
   (X מ-`progress_snapshots` מאז יום ראשון — state חדש `weekSeconds`, נשלף באותה
   קריאה של `todaySeconds`) + **"הודעה מהמנהל/ת"** = `daily_brief.notes` כשקיים.
   זה הבית של פיצ'ר "הודעות מהמנהל לצוות" מהסקיצה — כרגע ניזון מהערת הבריף.
2. **שני כרטיסים**: "העדכון היומי" (✓ קראתם / 0/1 טרם נקרא / — אין עדכון ⇒ טאב
   יומי) ו"למידת {קטגוריה מומלצת}" (אחוז + פס ⇒ `startProgressive`; נפילה לסבב
   כללי כשהכל עבר).
3. **PromoCarousel נמחק** — הקרוסלה של שקף-אחד הוחלפה בכרטיס העדכון היומי.

תוקן גם: `refetchWeekly()` נוסף לאפקט הטעינה — הדירוג השבועי היה ריק אחרי רענון
(נשלף רק אחרי אירוע ניקוד/realtime) למרות שהשורות ישבו ב-DB.

## 🎓 2026-08-20 (המשך) — מבחנים קשוחים + מבחן תפריט מלא + Tutorial ממוקד-מטרה

בקשת המשתמש: "האפליקציה מטרתה שהעובדים יעברו את המבחן — המשחקים הם רק האימון."
1. **QuizExam**: 8 ⇒ **20 שאלות** (`DEFAULT_DECK_SIZE`), **שעון 12 שניות לשאלה**
   (`SECONDS_PER_QUESTION`) — פקיעת זמן נספרת כטעות ומתקדמת לבד. השעון קופא בזמן
   פידבק ומתאפס כל שאלה. prop חדש `deckSize`.
2. **CategoryExam**: 4 ⇒ **12 מנות**, 45 ⇒ **25 שניות למנה**.
3. **מבחן התפריט המלא** — mode חדש `general_exam` ב-MainApp: QuizExam על כל
   התפריט, `deckSize = exam_config.general_exam_questions` (עמודה חדשה, ברירת
   מחדל 40, CHECK 10-80). כפתור גרדיאנט טורקיז בראש טאב התפריט. נרשם ל-
   `exam_results` עם `category='general'` (label בצד המנהל: "מבחן התפריט המלא").
4. **WelcomeTutorial**: שקף הסיום הוחלף ב"המטרה: לעבור את מבחן התפריט" —
   המשחקים הם אימון, הציונים הם התעודה שהמנהל רואה.
5. **צד המנהל**: הגדרת אורך המבחן (20/30/40★/60) ב-LearningPathSettings
   (`DEFAULT_PATH.general_exam_questions` + טעינה + `...path` בשמירה), ומסגור
   המטרה בתחנות ה-GuidedTour (צוות + הגדרות).

## ✨ 2026-08-20 (המשך) — פוליש GUI + פרוטוקול הפסקות + מנות חדשות בברכה

1. **פוליש GUI** (בלי שינוי פלטה): אייקון לכל משחק בגריד התרגול (`GAME_ICONS`,
   ליד `MODE_LABELS`), כפתורי משחק 44px, שני כרטיסי הגרדיאנט בגובה שווה (CTA
   מוצמד לתחתית עם mt-auto), רדיוס אחיד rounded-2xl לכרטיסים ראשיים (בית/יומי/
   תפריט/מדדים), ומעבר fade בין טאבים (`key={tab}` + animate-fadeIn).
2. **פרוטוקול הפסקות בתרגול הפרוגרסיבי** (`CHECKPOINT_EVERY = 10`): כל 10
   דירוגים — מסך "עוד סיבוב או הפסקה?"; כשכל ההיקף הובן — "סיימת ללמוד X!
   רוצה לגשת למבחן?" עם מעבר ישיר למבחן הקטגוריה (prop חדש `onExam`; `prog`
   שומר עכשיו `catKey`). "לא עכשיו" ⇒ עוד 10 כרטיסיות רענון ואז שואל שוב.
   הטוסט "🔄 רענון" הוסר — הצ'קפוינטים החליפו אותו.
3. **בועת הפתיחה מתעדפת מנות חדשות**: כשיש `newDishes` (חלון 21 יום + לא נגעו)
   השורה הופכת ל"נשאר לך ללמוד X מנות חדשות בתפריט 🍽️" + כפתור "ללמוד את
   החדשות ←" (כרטיסיות scoped). בלי חדשות — שורת הדקות השבועית כרגיל.
   ℹ️ "1 דקות" תוקן ל"דקה אחת".

### 🎨 מצב הצבעים הסופי של מסך הבית (2026-08-20, אישור משתמש)

בועת הפתיחה = **אמרלד המותג** (`#0F5C46`⇒`#0a3d2f`, טקסט קרם `#EEF0F6`, כפתור קרם) ·
כרטיס הלמידה = גרדיאנט **ירוק-אמרלד** (`#22c08c`⇒`#0F5C46`) · **העדכון היומי = סגול**
(`#8b5cf6`⇒`#6d28d9`; דף היומי קיבל מסגרת+פס סגולים תואמים) · כפתור הכרטיסיות ופס
ההתקדמות ירוקים · **הניווט התחתון נשאר מקורי** (ניסיון למתג אותו באמרלד נדחה —
"נראה רע"). ⚠️ לפי עקרון הטפטים: הגרסאות הקודמות (כתום לעדכון, סגול ללמידה,
בועה כהה) חיות בהיסטוריית הסשן/git.

## ✉️ 2026-08-20 — הודעות מהמנהל + משימות שבועיות/חודשיות (commit `8e198a7`)

שתי משימות שהגיעו מסשן אפליקציית המנהלים (הפיצ'רים חיים שם, ה-DB מוכן,
בצד המלצר לא היה צד קולט).

**`src/components/ManagerMessages.jsx`** — ההודעה האישית שהמנהל שולח למלצר
ספציפי (`menu_app.team_messages`, כפתור ✉ ברשימת "מי למד היום") מוצגת עכשיו
**ראשונה במסך המשימות**, מעל בועת הברכה: מי כתב, מה כתב, וכפתור "קראתי"
שמעדכן `read_at`. הטון הוא הודעה אישית, לא התראת מערכת.
ℹ️ **`read_at` נצרך בצד המנהל** (commit `c496023` שם): כפתור ההודעה מבחין
אפור (לא נשלחה) · כתום (נשלחה) · ירוק ✓ (נקראה). כלומר האישור הזה אינו מקומי —
שינוי בו משנה מה המנהל רואה.
ℹ️ אחרי האישור ההודעה **נעלמת** (הוסכם בין שני הסשנים): זו תזכורת נקודתית ולא
היסטוריית צ׳אט. אם יצטברו הודעות שכדאי לחזור אליהן — מקום ייעודי (מסך המדדים),
לא שורה גמורה שנשארת במסך המשימות.
⚠️ ה-ack נכתב לשרת **לפני** שההודעה נעלמת מהמסך — לקח באג הצ'קליסט: ✓ אופטימי
שלא הגיע לשרת נראה בדיוק כמו הצלחה. ה-RLS ממילא מצמצם ל-`session_member()`.

**תקופות ב-`useShiftTasks`** — `shift_tasks.kind` כולל מ-2026-08-20 גם
`weekly`/`monthly`, ומשימה שבועית שסומנה ביום שני חזרה כל בוקר כלא-מסומנת.
הסימון נשמר **תמיד ביום שבו נעשה** (`done_date`), ולכן ה**קריאה** נושאת את
התקופה: `periodStart(kind)` — weekly ⇒ מיום ראשון האחרון · monthly ⇒ מה-1
בחודש · השאר ⇒ היום. הקריאה מושכת מה-1 בחודש ומסננת כל שורה לפי התקופה של
**המשימה שלה**.
⚠️ **תחילת שבוע = יום ראשון**, בדיוק כמו `weekly_scores` ב-SQL וב-JS. שני
גבולות שבוע שונים באותו מוצר הם באג שקשה מאוד לראות.
`PERIOD_LABEL` מסמן "משימה שבועית"/"משימה חודשית" על השורה — "בוצע ✓" על
משהו שחוזר פעם בשבוע מבלבל בלי זה. המפריד שונה מ"בוצע היום" ל"בוצע".

נבדק חי מול CREWDEMO: שבועית מ-16.8 וחודשית מ-5.8 נשארו מסומנות, יומית
שסומנה אתמול חזרה לרשימה הפתוחה, ההודעה נעלמה אחרי אישור ולא חזרה ברענון.

## 🎓 2026-08-20 (לילה) — מבחן התפריט המלא נבנה מחדש כתרחישי שירות

בקשת המשתמש: שהמבחן הכללי יהיה כמו מבחני הקטגוריות — **להרכיב מנות** — ובנוסף
שאלות מהשטח ("אישה בהריון רוצה המלצה למנה ראשונה", "תמליץ על 3 רולים עם סלמון",
ואז צריך לדעת את **השם המלא** של המנה).

**`src/lib/serviceScenarios.js`** (לוגיקה טהורה, `node tests/scenarios.test.mjs`) —
חמישה בונים, כל אחד מחזיר `null` כשהתפריט לא יכול לשאת שאלה עם תשובה אחת בלבד:

| kind | השאלה | איך נשמרת תשובה יחידה |
|---|---|---|
| `compose` | "מה יש ב<מנה>?" — צ'יפים, סט מדויק | מסיחים = מרכיבים מאותה קטגוריה שאינם במנה |
| `pregnancy` | "אורחת בהריון מבקשת המלצה מ<קטגוריה>" | הנכונה `pregnancy=[]`, **כל** המסיחים מסומנים |
| `allergy` | "אורח אלרגי ל<אלרגן> — מה אפשר להגיש?" | 3 מסיחים מכילים; הנכונה **חייבת להצהיר אלרגנים** |
| `multi` | "המליצו על 3 מנות עם <מרכיב>" | נבנית רק כשיש **בדיוק 3** בקטגוריה; מסיח נפסל אם המרכיב מוזכר גם בשם/תיאור |
| `pitfall` | "איזו מהן מכילה <מוקש>?" | נבנית רק כשיש **נושא אחד** |

⚠️ **הכלל שמחזיק את הכל**: `buildMenuExamDeck` פוסל בסוף כל שאלה חד-תשובתית שיש בה
יותר מנכונה אחת — גם אם הבונה חשב אחרת. בשאלת הריון או אלרגיה "תשובה נכונה שנייה"
לא מלמדת שהאפליקציה טועה, אלא משהו גרוע יותר (שורה 16 ב-`QUESTION-QUALITY.md`).
⚠️ **`mentions()` בודק מילים שלמות ולא substring**, וגם את השם והתיאור — מנה
שהמרכיב מופיע בשמה לא כשירה כמסיח "שלילי" גם אם רשימת המרכיבים שלה חלקית.
⚠️ **סט מדויק, בלי ניקוד חלקי** — אחרת "לבחור הכל" עובר את "המליצו על 3".

**`src/games/MenuExam.jsx`** — מחליף את `QuizExam` ב-mode `general_exam`. שעון לכל
המבחן שנגזר מסוג השאלות (`compose` 35ש', `multi` 30, השאר 20), תג סוג שאלה, ואחרי
תשובה מוצגת **הסיבה לכל אופציה** ("מכילה דג נא" / "אין בה סויה"). ציון = אחוז
השאלות, סף מעבר **70 כמו CategoryExam**.
⚠️ `exam_config.pass_threshold` הוא **אחוז השליטה שנדרש כדי לגשת** למבחן קטגוריה,
לא ציון עובר — אל תחברו אותו לסף המעבר.

נבדק חי על "סטודיו תל אביב 2026" (187 פריטים): 40 שאלות, כל חמשת הסוגים הופיעו,
ו"אורחת בהריון מבקשת המלצה מניגירי" החזירה ניגירי אבוקדו כנכונה מול שלושה ניגירי
דג נא. נתוני ה-QA נמחקו.

### ⭐ עוד חמישה סוגי שאלות למבחן — הכל מתוך מאגר (2026-08-20, לילה)

בקשת המשתמש: "בלי שאלות פתוחות — שהכל צריך להשלים ממאגר מסוים." כלומר גיוון בלי
תשובות חופשיות: כל שאלה נענית בבחירה מתוך בנק אפשרויות שנגזר מהתפריט.

| kind | השאלה | מקור הבנק | השער שמונע דו-משמעות |
|---|---|---|---|
| `menugroup` | "באיזה תפריט נמצא X?" | `menu_group` | נבנית רק כשיש **3+ תפריטים** |
| `price` | "כמה עולה X?" | מחירים אמיתיים מאותה קטגוריה | **מחירים שונים בלבד** — שתי אופציות עם אותו מספר = שתי תשובות נכונות |
| `describe` | "אורח מתאר: … על איזו מנה?" | שמות מנות | `maskNameLeak` + דילוג אם נשארו <4 מילים |
| `allergenset` | "אילו אלרגנים יש ב-X?" | 8 האלרגנים | **רק מנה שהצהירה אלרגנים** — רשימה ריקה יכולה להיות "אין" או "לא מילאו" |
| `order` | "מה יוצא ראשון מהשולחן?" | 3 מנות מ-3 קטגוריות | **רק מ-`exam_config.category_order` של הבעלים** |

⚠️ **`order` לא מנחש course משם הקטגוריה.** "ראשונות" נשמע לנו כמו מנה ראשונה, אבל
המסעדה שכתבה את התפריט היא הסמכות היחידה על מה יוצא ראשון — תשובה שגויה כאן מלמדת
את המלצר משהו לא נכון על השירות של המסעדה שלו. אין `category_order` ⇒ אין שאלה.
`MenuExam` מקבל `categoryOrder` כ-prop מ-`exam_config`.

נבדק חי על סטודיו 2026 (מבחן של 40 שאלות): **8 סוגים הופיעו בפועל** — הרכבת מנה,
המלצה לאורח, אלרגיה, אלרגנים במנה, מחיר, איפה בתפריט, סדר הגשה, אורח מתאר מנה.
`node tests/scenarios.test.mjs` בודק לכל סוג את השער שלו (מחירים ייחודיים, אין דליפת
שם לתיאור, הנכונה היא הקטגוריה המוקדמת ביותר בסדר של הבעלים).

## 👆 2026-08-20 (לילה) — הסיור נעשה אינטראקטיבי + מסלול הלמידה נאמר בקול

בקשת המשתמש: הסיור לא ברור, המלצרים לא יודעים מה לעשות כשהם מגיעים למסך —
"צריך ממש להכריח אותם ללחוץ עליו כדי לפתוח אותו", ולהסביר את שלבי ההתקדמות.

**`AppTour.jsx` נכתב מחדש.** קודם זו הייתה ערימת כרטיסי הסבר עם "הבא": המלצר קרא
7 פסקאות, לחץ 7 פעמים, והגיע למסך שמעולם לא נגע בו. עכשיו שלב יכול לציין **אלמנט
אמיתי** (`target`, לפי `data-tour`): המסך מוחשך **חוץ ממנו**, ואין כפתור "הבא" —
הדרך היחידה להתקדם היא **להקיש על הדבר עצמו**.

- **החור הוא חור אמיתי**: ארבעה מלבנים מוחשכים סביב האלמנט (לא overlay אחד שקוף),
  ולכן ההקשה נוחתת על האפליקציה. טבעת ירוקה פועמת מסמנת את היעד.
- 9 שלבים שמסבירים את **המסלול**: קוראים את התפריט ⇒ מתרגלים בכרטיסיות ⇒ מבחן לכל
  קטגוריה ⇒ ורק בסוף מבחן התפריט המלא.
- ⚠️ **`firedRef` — התקדמות אחת לכל שלב.** ההקשה ממתינה 260ms כדי שהמסך יתחלף,
  והקשה שנייה בחלון הזה הייתה מתזמנת `go(i+1)` עם `i` ישן ומחזירה את הסיור אחורה.
- ⚠️ **איפוס דריל-דאון רק כשהטאב באמת מתחלף**: שני שלבים רצופים חולקים טאב ("הקישו
  על תפריט" ⇒ "הקישו על קטגוריה"), ואיפוס בכל שלב **ביטל את ההקשה שהמלצר בדיוק
  התבקש לעשות**. `browseKey` מרנדר מחדש את MenuBrowser רק על מעבר טאב אמיתי.
- אין יעד על המסך (מסעדה עם תפריט אחד) ⇒ נפתח כפתור "לא רואים את זה? המשיכו הלאה".

**טאב תרגול ובחינה — שלוש בקשות נוספות:**
1. **הבאנר "מבחן התפריט המלא" לא מוצג כלל** עד שעוברים את כל מבחני הקטגוריות
   (`generalExamCard()` מחזיר `null` כשנעול). באנר "אי אפשר עדיין" בראש המסך הוא
   הדבר הראשון שמלצר חדש קורא, והוא ממסגר את כל הטאב כחסום.
2. **"לתרגול תפריט המסעדה ←"** על כל כרטיס תפריט — בלשון שהמלצר משתמש בה.
3. **"הגיעו ל-50% כדי להיבחן" נמחק לגמרי.** מלצר לא יודע מה זה 50%, וזה גם השאיר
   כפתור מת בכל קטגוריה. עכשיו: אין שורת מבחן עד שהוא זמין, וברגע שכן —
   **"אתם מכירים מספיק מ<קטגוריה> — להיבחן עכשיו?"** עם שתי אפשרויות:
   "למבחן <קטגוריה>" / "להמשיך ללמוד".

**שעון מבחן הקטגוריה הוגדל** ב-`QuizExam` (14px + `Ns`, היה אייקון 12px ומספר יחף)
כדי שיהיה ברור שמדובר במבחן מתוזמן. `CategoryExam` כבר הציג `⏱ mm:ss`.

נבדק חי על סטודיו 2026: הסיור עבר את כל 9 השלבים כשההתקדמות בשלבים 2/3/5 הגיעה
**רק מהקשה על היעד האמיתי**; מבחן ראשונות נפתח עם `⏱ 04:35`; ואחרי שקטגוריה הגיעה
ל-100% הופיעה שאלת "להיבחן עכשיו?" בעוד שאר הקטגוריות בלי שורת מבחן כלל.

## 🕐 2026-08-20 (לילה) — משמרת ותפקיד, המשך אוטומטי, בוחן בכתיבה חופשית

חמש בקשות משתמש בסבב אחד:

**1. שער כניסה יומי — "אתם במשמרת היום?"** (`ShiftGate` ב-`ShiftPicker.jsx`).
נשאל פעם ביום, לפני שער הבריף. "כן" ⇒ שאלון פתיחה/ערב (+תפקיד מלצר/ברמן בפעם
הראשונה) ⇒ שער הבריף כרגיל. "לא היום — רק ללמוד" ⇒ `shift='none'`: **בלי** שער
בריף, בלי משימות יומיות — רק קבוצת הלמידה. התשובה נשמרת ב-localStorage עם תאריך
(`lib/shiftChoice.js`) — מחר השאלה חוזרת. צ'יפ "פתיחה · מלצר/ית · לשינוי" בראש
מסך המשימות משנה את התשובה בכל רגע.
- `taskFitsShift(kind, shift)`: opening⇒רק פותחים, closing⇒רק סוגרים,
  shift/weekly/monthly⇒כל מי שעובד, training וכל kind לא מוכר ⇒ **תמיד מוצג**.
- **עמודת `shift_tasks.role`** (מיגרציה `shift_tasks_role_column`, NULL=כולם,
  waiter/bar) + `taskFitsRole`. ⚠️ ברירת המחדל של שני המסננים היא "להציג" —
  ערך חדש בצד המנהל לעולם לא מעלים משימה.
- ⚠️ נתוני סטודיו תוקנו: שתי משימות ותיקות נשאו "פתיחה:"/"סגירה:" **בכותרת** עם
  `kind='shift'` — סוננו לא נכון עד שתויגו מחדש. שתי משימות בר תויגו `role='bar'`.

**2. סיום משימה פותח אוטומטית את הבאה** (`TasksTab`): "ביצעתי" ⇒ נפתחת המשימה
הפתוחה הבאה (גיליון להוראת מנהל, מסך למשימה עם יעד). ⚠️ המשימה שסומנה הרגע עדיין
"פתוחה" ב-props של אותו render — מדלגים עליה מפורשות לפי id.

**3. פילת "בוצע" אחת בתחתית המסך כולו** — שורות גמורות לא יושבות יותר בין משימות
המשמרת לקבוצת הלמידה. קבוצה שהושלמה מציגה "הכל בוצע כאן ✓".

**4. קבוצת הלמידה מכוונת קודם לתפריט**: מי שדירג פחות מ-10 מנות מקבל משימה
ראשונה "לעבור על התפריט ולהכיר את המנות" (טאב תפריט); משימת הקטגוריה מתחלפת לפי
זמן הלימוד היום — מתחת לחצי מהיעד ⇒ "ללמוד X מהתפריט", מעליו ⇒ "לתרגל X".

**5. בוחן קטגוריה בכתיבה חופשית** (`CategoryExam` + `lib/typedGrading.js` +
`tests/typed.test.mjs`): במקום צ'יפים — עד 7 שדות כתיבה, המלצר כותב מרכיבים
**מהזיכרון**. אלרגנים נשארו צ'יפים (רשימה סגורה — בטיחות).
- **איות אינו ידע**: התאמה עמומה — נירמול גרשיים (גינגר=ג'ינג'ר), קיפול אותיות
  סופיות, Levenshtein לפי אורך (3 אותיות=מדויק, 6+=עד 2), ומילת **ראש** חייבת
  להתאים ("בצל" מזהה "בצל ירוק", "ירוק" לבדו לא).
- **היזכרות קשה מזיהוי ⇒ ניקוד מקל**: 70% מהמרכיבים = ציון מלא; המצאה ‑0.15.
  זה לא מחזיר את פרצת ה-recall מ-2026-08-11 — אין רשימה להעתיק ממנה, ויש קנס.
- נבדק חי: "גינגר" זוהה, "שוקולד" נפסל, הפספוסים הוצגו.

**וסבב עברית + מינוח**: "לקבל שולחנות"⇒"לקפל" (DB), עברת/אתה⇒עברתם/אתם,
"הנסיונות"⇒"הניסיונות", וכל 400+ המחרוזות נסרקו. **מינוח אחיד: בוחן = קטגוריה,
מבחן = התפריט המלא** — הוחל בכל האפליקציה (כפתורים, כותרות, סיור, tutorial,
מסך הסיום של MenuBrowser: "לתרגול מנות X לקראת הבוחן").

### ⏳ משימות "להיום בלבד" — `shift_tasks.expires_on` (2026-08-22)

עמודה חדשה מצד המנהל (commit `93e02f0` שם): `expires_on date` — המנהל מוסיף משימה
ל**יום אחד**. null = קבועה. שורות שפגו **לא נמחקות** (היסטוריה + יעד FK של
`shift_task_done`), ולכן הקריאה ב-`useShiftTasks` מסננת:
`.or('expires_on.is.null,expires_on.gte.<today>')`. שורה עם `expires_on` מקבלת
badge כתום "להיום בלבד" (`todayOnly` ב-dayTasks). נבדק חי: משימת היום מוצגת עם
ה-badge, משימת אתמול לא מוצגת.

## 🔤 פנייה ביחיד ניטרלי — החלטת סגנון (2026-08-22, החלטת משתמש)

השאלה של המשתמש: "למה ברבים? למה לא עברת/ה?" — והוא צודק פעמיים: (1) **עבר גוף
שני יחיד נכתב זהה לזכר ולנקבה** (עברת, למדת, ידעת, פספסת) — אין שם בכלל בעיית
מגדר; (2) מסך הבית ממילא דיבר ביחיד ("שלום X, השבוע למדת") — האפליקציה הייתה
מעורבבת.

**הקונבנציה מעכשיו** (הוחלה על כל ~30 המחרוזות הפונות):
1. **עבר יחיד** כשאפשר — ניטרלי מולד: "עברת!", "זיהית נכון", "פספסת".
2. **הווה/עתיד** — עדיף ניסוח סתמי על ערימת לוכסנים: "יש לך מספיק ידע ב-X"
   במקום "אתם מכירים מספיק", "אילו משימות יוצגו" במקום "תראו", "הקשה = תרגול"
   במקום "לחצו לתרגול".
3. **לוכסן רק כשאין מנוס** בהוראה ישירה: "הקש/י", "בחר/י", "כתוב/כתבי".
4. **רבים סתמיים נשארים** — "עוברים על התפריט, מתרגלים, נבחנים" הוא תיאור
   השיטה, לא פנייה לאדם.

⚠️ בדיקת ה-leftovers: `grep -rn "עברתם|קראתם|שלכם|הקישו|לחצו|אתם " src/` צריכה
להחזיר 0 בקבצים החיים (למעט הערות קוד ושלדים מהעבר).

## 👤 פרופיל חד-פעמי: מגדר + תפקיד, ומשמרת יומית (2026-08-22)

בקשת המשתמש: בתהליך הכניסה לשאול פעם אחת (1) זכר/נקבה (2) מלצר/ברמן/**גם וגם** —
ולא לשאול את זה שוב כל יום; רק "גם וגם" נשאל בכל יום במה הוא היום. בנוסף אופציית
משמרת שלישית: **"בלי משמרת"** — לא פתיחה ולא ערב, רק לימוד **ועדכון יומי**.

- **`ProfileGate`** (ShiftPicker.jsx) — פעם אחת: מגדר ⇒ תפקיד. נשמר ב-localStorage
  פר-חבר (`menu-app-gender`/`menu-app-role`, בלי תאריך — קבוע). "שינוי תפקיד קבוע
  או פנייה" בכרטיס עריכת המשמרת מאפס ומריץ מחדש.
- **`ShiftGate`** יומי: פרופיל "גם וגם" ⇒ קודם "במה את/ה היום?" (נשמר
  `menu-app-role-today` עם תאריך); אחרת ישר לשאלת המשמרת. שלוש אפשרויות:
  פתיחה / ערב / בלי משמרת. **הוסר שלב ה"כן/לא במשמרת?"** — "בלי משמרת" החליף אותו.
- ⚠️ **"בלי משמרת" ≠ "לא עובד"**: שורת "לקרוא את העדכון היומי" מוצגת **לכל** תשובת
  משמרת (הוסר התנאי `myShift !== "none"` מהמשימה). רק שער הבריף החוסם מדולג.
  צ'קליסטים של פתיחה/סגירה/משמרת עדיין מוסתרים (`taskFitsShift`).
- **`gz(text)` — עותק ממוגדר** (shiftChoice.js): מפת טוקנים ("הקש/י"⇒הקש/הקשי,
  "מלצר/ית", "בוא/י"...) שמוחלת בזמן רנדור לפי המגדר השמור. ⚠️ **מפה מפורשת ולא
  regex גנרי** — "המנהל/ת" הוא המגדר של המנהל, לא של המלצר, ואסור לגעת בו.
  ⚠️ gz ברמת מודול רץ לפני שהפרופיל קיים (BaselineIntake) — להחיל **בזמן רנדור**.
- `myRole` האפקטיבי = תפקיד הפרופיל, או התשובה היומית כשהפרופיל "גם וגם".

נבדק חי: נקבה+גם וגם ⇒ "במה **את** היום?", "מלצרית/ברמנית" בלי לוכסן; "בלי משמרת"
⇒ שורת העדכון היומי כמשימה 1 + משימות למידה, בלי צ'קליסטים; רענון ⇒ אף שאלה לא
חוזרת (הפרופיל והמשמרת שמורים), הצ'יפ "בלי משמרת · ברמנית · לשינוי".

## 🎨 2026-08-23 — הסיור לומד להקיש, ומקרא הצבעים פעם ביום

**`ColorKey.jsx` (חדש)** — מקרא הצבעים מוצג **פעם ביום, לפני התרגול הראשון**
(`needsColorKey`/`markColorKeySeen`, localStorage עם תאריך; הגייט יושב מעל כל
`mode` ב-MainApp). הסיבה: שלוש תגיות באותו גודל נקראות כשלוש אזהרות שוות —
אדום (אלרגיה) יכול לשלוח אורח לבית חולים, צהוב (חריף/כוסברה) הוא עניין של טעם.

**הסיור (`AppTour.jsx`) — 12 שלבים אחרי סבב משוב על הגרסה החיה:**
- שלבי הטאבים מסמנים קודם את **הטאב בסרגל התחתון** (`nav-categories`/`nav-learn`),
  ורק אחר כך את התוכן — המלצר לומד איפה הדברים, לא מועבר אליהם.
- "כל מנה נפתחת בגדול" היה פסקה בלי מה לעשות איתה. פוצל: הקשה על **מנה אמיתית**
  (`data-tour="browse-dish"` על הפריט הראשון ברשימה) ואז כרטיס עם **דוגמה**
  שקוראת את התגיות של המנה שעל המסך.
- 🔴 **"התפריט בסיור נתקע"**: שלב רשימת התפריטים נפתח לפעמים כשה-MenuBrowser עדיין
  בתוך דריל-דאון מהרצה קודמת ⇒ זרקור על כלום. שלב שדורש רמה עליונה נקייה מסמן
  `reset: true`, ו-MainApp מכבד אותו (הכלל הישן "לאפס רק כשהטאב מתחלף" נשאר —
  הוא תיקון לבאג אחר, שאיפוס בכל שלב ביטל את ההקשה שהמלצר התבקש לעשות).
- **"אחורה" מזיז גם את המסך**, ומדלג אחורה מעל שלבים שמסומנים `deep` (שלבים שחיים
  בתוך דריל-דאון שרק הקשות המלצר יכולות להגיע אליו) — אחרת הכרטיס מדבר על מסך
  שהאפליקציה לא יכולה להעמיד.
- 🔴 **`z-50` מול `z-50`**: מסך המנה המלא הוא `fixed inset-0 z-50` בדיוק כמו הסיור,
  ומאוחר יותר ב-DOM ⇒ הסיור היה **בשלב 6 אבל בלתי נראה** מאחוריו. הועלה ל-`z-[60]`.
  ⚠️ שני overlays באותו z הם באג שנראה כמו "הסיור נעלם".
- **ניסוחים** (בקשות משתמש): "כרטיסיות" מסביר מה יש בכרטיס — הופכים רק כשיודעים,
  ובגב התיאור, המרכיבים, האלרגיות והרגישויות; שלב המבחן ויתר על "הציונים הם התעודה
  שלך" ואומר שזה מבחן התפריט ושאת המבחן עצמו עושים במסעדה.

**"משימות היום" ⇒ "משימה יומית"** (TasksTab + הסיור).

### 🔁 סבב בדיקה מלא של הסיור (2026-08-23, אחרי הפריסה) — שלושה באגים

הסיור נבדק שלב-שלב על סטודיו 2026, קדימה ואחורה. מה שנתפס:

1. 🔴 **שלב "טאב התרגול" הצביע על סרגל ניווט שהיה מכוסה**: השלב הקודם משאיר את
   המלצר בתוך מסך המנה המלא (`fixed inset-0`), הזרקור נמדד על ה-nav שמאחוריו,
   וההקשה נחתה על כפתור "הבאה" של המנה. השלב קיבל `tab`+`reset` — הריסט סוגר את
   מסך המנה ומחזיר את הסרגל למסך. ⚠️ **שלב שמצביע על הניווט התחתון חייב לוודא
   שאין overlay מלא-מסך פתוח.**
2. 🔴 **הסיור נעלם כשנפתח מסך המדדים**: `MetricsScreen` הוא early return ב-MainApp,
   כלומר הוא מחזיר עץ אחר לגמרי — והסיור, שרונדר בתוך העץ הראשי, פשוט לא היה שם.
   נבנה `tourNode` **מעל** ה-early returns ומרונדר בשני הענפים.
3. 🔴 **ואז הסיור חזר לשלב 1**: אותו רכיב תחת הורה אחר = unmount+remount, וה-state
   המקומי (`i`) נמחק. **אינדקס השלב הורם ל-MainApp** (`tourStep`/`setTourStep`,
   `AppTour` הפך לרכיב מבוקר). ⚠️ **state שחייב לשרוד מעבר בין ענפי רינדור לא יכול
   לחיות ברכיב עצמו.**

**שני שלבי סיום חדשים**: הסיור *נכנס* למסך המדדים (`data-tour="metrics"`) ומסיים
עליו, מול נתונים אמיתיים, במקום לתאר אותו מבחוץ. **`onReplayTour`** במסך המדדים
מריץ את הסיור שוב — עד עכשיו הוא היה חד-פעמי ביום הראשון בלבד.

**ניסוחים**: "בפינה הימנית" ⇒ שמאלית (RTL — כפתור הגרף בשמאל); "המנהל/ת שלחו" ⇒
"המשימות מהמנהל/ת"; "נפתח קטגוריה" ⇒ "כאן בוחרים מה לתרגל" (היעד הוא תפריט, לא
קטגוריה); "בחרו תפריט כדי לעיין בו" ⇒ ניסוח סתמי; "לעבור על התפריט ולהכיר את
המנות" ⇒ **"לעבור על התפריט"** (בקשת משתמש — ארוך מדי).

**בועת הפתיחה כבר לא סופרת חובות** (בקשת משתמש): "נשאר לך ללמוד 187 מנות" נקרא
כמו חוב ביום הראשון — בדיוק היום שבו מחליטים אם לפתוח את האפליקציה שוב. עכשיו:
0% ⇒ "מתחילים מהתפריט — נעים להכיר 🍽️" + **"לפתוח את התפריט ←"** (לטאב התפריט,
לא לכרטיסיות — קוראים תפריט לפני שמתרגלים אותו); מעל 0 ⇒ "כבר X% מהתפריט אצלך 💪".

## 👁️ 2026-08-23 — מצב צפייה למנהל (`?preview=<team_code>`)

בקשה מסשן אפליקציית המנהל: "תצוגת מלצר" (iframe בתוך אפליקציית המנהל) טוענת עכשיו
`https://shiftcrew-waiter.vercel.app/?preview=<TEAM_CODE>` והמנהל אמור להיכנס בלי
להקליד קוד ושם.

- **RPC חדש `menu_app.team_preview(p_team_code)`** (SECURITY DEFINER) — מחזיר את פרופיל
  המסעדה + טוקן סשן עם **`team_member_id = NULL`** ו-`role='preview'`, תפוגה 12 שעות.
  ⚠️ **בכוונה לא `team_join`**: הוא יוצר `team_members`, וחבר צוות מזויף בשם "תצוגת
  מלצר" היה מופיע למנהל ברשימת הצוות ובסטטיסטיקות שלו.
  ⚠️ נדרשה הרחבת `app_sessions_role_check` ל-`owner/team/preview`.
- **למה NULL מספיק כהגנה**: כל policy כתיבה בסכמה נשענת על `session_member()`
  (menu_progress · leaderboard · weekly_scores · daily_brief_reads · shift_task_done),
  ולכן טוקן בלי member פשוט לא מתאים לאף שורה. אומת חי: קריאת תפריט עובדת, INSERT
  ל-leaderboard מוחזר `42501 violates row-level security`.
- **צד ה-client**: `App.jsx` בודק `?preview` **לפני** הסשן השמור — מנהל שהתחבר פעם
  כמלצר באותו דפדפן היה רואה בתצוגה מסעדה אחרת לגמרי. הסשן **לא נשמר** ב-localStorage.
  קוד לא תקין ⇒ הלוגין הרגיל.
- **MainApp**: דגל `preview` יחיד — מדלג על שלושת השערים (פרופיל/משמרת/בריף — המנהל בא
  לראות, לא לענות), מדלג על כתיבות (`learnItem` עוצר, `last_seen_at` לא נכתב), וכל
  שאילתה שמסוננת לפי חבר צוות מדולגת (`.eq(col, null)` אינה שאילתה שרוצים לשלוח).
  משמרת ברירת מחדל `opening` כדי שיהיה צ'קליסט להראות, בלי צ'יפ שינוי.
- באנר ירוק קבוע: "מצב צפייה — כך המלצרים רואים את האפליקציה. שום דבר לא נשמר."
- **מקרא הצבעים (ColorKey) מוצג גם בתצוגה** (החלטת סשן המנהל) — אבל **בכל טעינה**
  ולא פעם ביום: אין member למתג עליו, וכל מטרת התצוגה היא שהמנהל יראה בדיוק את מה
  שהמלצר רואה. iframe שנטען מחדש מציג אותו שוב — מכוון.

אומת אחרי הבדיקה: 6 חברי צוות בסטודיו 2026 (ללא שינוי), 0 שורות `menu_progress`
עם member ריק, והכתיבה האחרונה בטבלה היא מאתמול.

## 🎓 2026-08-23 — בוחן הקטגוריה נבנה בפעם השלישית: בחירה מתוך מסיחים אמיתיים

תגובת המשתמש על הגרסה הקודמת: **"זה כמעט בלתי אפשרי — תחשוב על משהו אחר שקשה לרמות בו."**
הצדק איתו: "סוכריות קרם ברולה" מכילה קרם וניל / קרמל / קולי פטל, ומלצר שכתב
"קרם ברולה, סוכר" — כלומר יודע מה המנה — קיבל **13%**. ידע תפריט אינו היכולת לדקלם
שמות ספקים.

**שלוש גרסאות, שני כשלים הפוכים:**
| גרסה | הכשל |
|---|---|
| טקסט חופשי מנוקד לפי אזכורים | recall בלי precision — "בחר הכל" קיבל 100% |
| הקלדה מהזיכרון (2026-08-20) | הכיוון ההפוך — תשובה נכונה בעליל קיבלה 13% |
| **בחירה מתוך 14 אפשרויות (עכשיו)** | — |

**למה קשה לרמות:** כל האפשרויות הן **מרכיבים אמיתיים של מנות אחרות באותה קטגוריה**,
לעולם לא מילות אוכל גנריות. "מה מזה נשמע כמו אוכל" לא עוזר — הכל אוכל שהמטבח הזה
מגיש; הדרך היחידה היא להבחין בין המנה הזו לשכנות שלה. גודל המאגר קבוע (14) כדי שמספר
הצ'יפים לא ירמז כמה נכונים.

⚠️ **מאגר משותף לכל המבחן נוסה ונדחה**: קטגוריה של 11 מנות מייצרת ~50 צ'יפים; חיפוש
בקיר טקסט מודד סבלנות, לא ידע. המאגר הוא **פר-שאלה**, והמסיחים הם השכנות.

⚠️ **`FP_COST = 1.5`** — בחירה שגויה עולה יותר מפספוס. עם Jaccard רגיל "לסמן הכל"
קיבל **50%**, ופורמט שבו כוח גס מזכה בחצי ציון אינו מבחן. זו גם השקלול הנכון בשירות:
מרכיב שנשכח גורם להיסוס, מרכיב שהומצא גורם לאמירה שקרית לאורח.
**נמדד חי** (בוחן ראשונות, סטודיו 2026): "לסמן הכל" ⇒ **32%**, תשובה נכונה ⇒ **100%**.

ℹ️ `src/lib/typedGrading.js` + `tests/typed.test.mjs` נשארו בריפו אף שאינם בשימוש —
מצב ההקלדה הוא "טפט" אפשרי למסעדה שתרצה אותו (ר' עקרון הטפטים ב-CLAUDE.md הראשי).

**תיקון נוסף באותו סבב**: `BaselineIntake` מריץ עכשיו `setCurrentGender` בעצמו —
הוא מרונדר מ-`App.jsx` לפני ש-MainApp מותקן, ולכן מלצר חוזר ראה "בוא/י נראה איפה
את/ה עומד/ת" למרות שהמגדר שמור. (חבר צוות חדש עדיין רואה לוכסנים — אין לו פרופיל.)

### 🔍 מבחן התפריט המלא — סבב בדיקה מלא (2026-08-23)

נבדק חי מקצה לקצה על סטודיו 2026 (חשבון QA עם כל בחני הקטגוריות שעברו, 40 שאלות,
שעון 15:49). **כל עשרת סוגי השאלות הופיעו בדק אחד**: הרכבת מנה · אורחת בהריון ·
אלרגיה · אלרגיות במנה · מחיר · המלצה לאורח · העדפת אורח · איפה בתפריט · אורח מתאר
מנה · סדר הגשה. השערים אומתו מול ה-DB בשלוש שאלות מדגמיות:
- הריון: 3 המסיחים באמת מסומנים (`בשר נא`/`דג נא`/`נבטים חיים`) והנכונה `[]`.
- אלרגיה לביצים: רק "בלאק אנד וויט" בלי ביצים — **והיא מצהירה אלרגנים** (שומשום),
  כלומר לא "תשובה נכונה כי לא מילאו נתונים".
- "3 מנות עם ג'ינג'ר מראשונות": בדיוק 3 קיימות בקטגוריה, והמסיחים נקיים ממנו.
- **"לסמן הכל" מוחזר כ-✗** (סט מדויק, בלי ניקוד חלקי) — לא ניתן לעבור בכוח גס.
- שורת `exam_results` עם `category='general'` נכתבת בסיום. אומת.

🔴 **באג שנתפס בבדיקה — "סדר הגשה" על משקאות**: השאלה שנבנתה הייתה
*"שולחן הזמין: שביל האבנים הצהובות · קמפרי · תה קר היביסקוס ליים. מה יוצא ראשון?"* —
שלושה משקאות. **לשאלה הזו אין תשובה אמיתית**: משקה מגיע לשולחן כשמוזגים אותו, וסדר
הקטגוריות של הבעלים לא אמור להכריע ביניהם. תוקן ב-`qServingOrder`: מועמד חייב להיות
**אוכל** — יש לו מרכיבים או תיאור. זה בדיוק הסימן שבו `MenuCheckQuiz` מזהה קטגוריה
"רזה", ושורות בר/שתייה קלה נופלות בו. ⚠️ נוספה בדיקה ב-`tests/scenarios.test.mjs`
(שלושה משקאות ⇒ `null`; אוכל ⇒ נבנית כרגיל).

**ניסוחים**: הפרומפטים של המבחן עברו ליחיד ("מה תגיד/י לו?", "אילו מנות תציע/י?"),
ומונה הבחירות תוקן ל-"נבחר 1" (היה "נבחרו 1").
**מקרא הצבעים לא מוצג לפני מבחן** — הוא כלי לימוד, ומבחן מתוזמן אינו הרגע לתת אותו.

**אפשרויות קצרות מוצגות כצ'יפים** (בקשת משתמש, כמו בבוחן הקטגוריה): רשימת מרכיבים
או אלרגיות בת 14 שורות מלאות היא כמעט מסך שלם, ובמבחן של 40 שאלות זה הרבה גלילה.
`compactOptions` = כל התוויות ≤20 תווים **ואף אחת לא נושאת `why`** ⇒ עטיפת צ'יפים;
אחרת שורות מלאות. ⚠️ שמות מנות נשארים שורות בכוונה — הם ארוכים, והם אלה שמציגים
את ההסבר לכל אופציה אחרי התשובה ("מכילה בשר נא" / "אין בה סיכון להריון").

## 🚻 2026-08-23 — שאלת המגדר הוסרה מהכניסה

בקשת המשתמש: **לא לשאול בכניסה זכר/נקבה** — לנסח טבעי, וכשאין ברירה לכתוב בלשון זכר.

- **`ProfileGate` ירד לשאלה אחת** — התפקיד (מלצר/ברמן/גם וגם). מסך המגדר נמחק,
  וכל צנרת ה-`loadGender`/`saveGender`/`GENDERS` הוסרה.
- **`gz()` נשאר** אבל ברירת המחדל שלו היא **זכר** (`currentGender = "m"`), כלומר
  הוא עכשיו מנקה לוכסנים מהמסך: "הקש/י" ⇒ "הקש", "מלצר/ית" ⇒ "מלצר". נוספו לטבלה
  `תגיד/י`, `תציע/י`, `שתכיר/י`, `לחץ/י`, `אתה/את`.
  ⚠️ **`המנהל/ת` נשאר כפי שהוא** — זה המגדר של המנהל, לא של המלצר.
- הפרומפטים של המבחן (`serviceScenarios`) עוברים דרך `gz` ב-`MenuExam`.
- העדיפות היא עדיין ניסוח שלא נזקק לכלום: עבר יחיד ("עברת", "הכרת") ניטרלי מולד.

**"הקשה = תרגול" ⇒ "לחץ כדי לתרגל"** בשורת הקטגוריה (בקשת משתמש).
⚠️ נתפס בזמן העריכה: `gz` **לא היה מיובא** ל-MainApp — `vite build` עבר, זה היה
`ReferenceError` בדפדפן. הלקח החוזר: build ירוק אינו אימות.

### 🔖 שלוש קבוצות אזהרה, כל אחת עם שם והסבר (2026-08-23)

בקשת משתמש: במקום כרטיס אחד "מה חשוב לדעת" — לפצל ל**אלרגיות / רגישות בהריון /
מוקשים**, ולהסביר מה זה מוקש. `FLAG_GROUPS` ב-`MenuBrowser.jsx`: כותרת, משפט הסבר
וצבע לכל קבוצה (אותם צבעים כמו ב-`ColorKey`). "מוקש" מוגדר במפורש: *לא מסוכן —
פשוט טעם שאורחים רבים מבקשים בלעדיו*.

### 🎓 הצעת בוחן בצ'קפוינט (2026-08-23)

אחרי 10 כרטיסיות, אם הקטגוריה חצתה את סף הבעלים — מופיעה **אפשרות שלישית**:
"יש לך מספיק ידע — לבוחן X", לצד "עוד סיבוב" ו"הפסקה". קודם היה צריך לצאת, לחזור
לרשימת הקטגוריות וללחוץ שם.
⚠️ `examReady` **מחושב בזמן רנדור** מ-`masteryById` ולא נלכד בפתיחת הסבב — המלצר
מדרג בדיוק עכשיו, והרגע שבו הקטגוריה חוצה את הסף הוא הרגע להציע.
🔴 **`scorePct` הועבר מעל ה-early returns**: ענף התרגול הפרוגרסיבי מחזיר לפני
ההגדרה שלו, וקריאה ל-const לפני שהוגדר היא `ReferenceError` (TDZ) שה-build **לא**
תופס — האפליקציה קרסה למסך "משהו השתבש" עד לתיקון.

### 🚪 סבב מלא על תהליך הכניסה (2026-08-23, בפרודקשן)

נבדק כמלצר חדש לגמרי, מסך-מסך: כניסה ⇒ 4 שקפי פתיחה ⇒ בוחן היכרות (21 שאלות) ⇒
פרופיל ⇒ משמרת ⇒ שער הבריף ⇒ בית + סיור. אין קריסות, והשערים מופיעים בסדר הנכון.
מה שנתפס:

1. 🔴 **לבוחן ההיכרות לא הייתה יציאה.** מסך הפתיחה מציע "אעשה את זה אחר כך", אבל
   אחרי "מתחילים" המלצר נעול ל-21 שאלות — מי שפתח את זה באמצע משמרת היה צריך לסגור
   את האפליקציה. נוסף קישור יציאה גם בתוך הבוחן (`onDone(null)` — נספר כדילוג).
2. 🔴 **"יש 166 מנות חדשות" בברכה למלצר חדש** — אותו מונה מייאש שהוסר, בתחפושת.
   "מנה חדשה" יש לה משמעות רק מול תפריט שכבר מכירים; מי שהצטרף היום לא נגע בכלום,
   וכל הייבוא נמצא בחלון 21 הימים. `newDishesWorthSaying`: מזכירים חדשות רק כשהן
   **עד חמישית מהתפריט** (או 5 מנות). מעל זה זה לא חדשות — זה התפריט, וזה תפקידו
   של מסלול הלמידה.
3. **לוכסנים שנשארו על המסך**: placeholder "הקוד שקיבלתם" (רבים) ⇒ "שקיבלת";
   צ'יפי התשובות בבוחן ההיכרות ("מכיר/ה חלק", "די בטוח/ה") רונדרו גולמיים — רק
   השאלה עברה דרך `gz`, עכשיו גם האפשרויות; והודעת השגיאה ב-TeamLogin נכתבה
   מחדש בניסוח סתמי (היא רצה לפני שקיים פרופיל, אז אין ל-gz על מה להסתמך).

ℹ️ נצפה ולא שונה: שקף התדריך הראשון מציג את `service_notes` המלא של המסעדה —
אצל סטודיו זה קיר טקסט שממלא מסך. זה תוכן של הבעלים, וההחלטה אם לקצר/לגלול היא
החלטת מוצר.

### 📐 הסיור: רוחב טלפון, קצב מהיר, וסיום בבית (2026-08-23)

שלוש בקשות משתמש על הסיור:
1. 🔴 **הכרטיס נמתח על כל רוחב הדפדפן.** ה-overlay הוא `fixed inset-0` (וזה נכון —
   ההחשכה צריכה לכסות את כל המסך), אבל **הכרטיס עצמו הוא חלק מהאפליקציה** וחייב
   לשבת באותה עמודה `max-w-md`. נוסף `mx-auto w-full max-w-md`. במובייל זה נראה תקין
   ולכן זה שרד — **צריך להסתכל גם ברוחב דסקטופ.**
2. **קצב**: `useTargetRect` נמדד כל 300ms והתקדמות אחרי הקשה חיכתה 260ms — כל שלב
   נפתח עם פעימה של כלום. ירדו ל-120ms בשניהם.
3. **מעבר חלק בין שלבים**: אחרי שהקצב הואץ, ההחלפה נראתה קופצנית. ארבעת מלבני
   ההחשכה והטבעת מקבלים `transition` של 180ms על top/left/width/height — הזרקור
   **גולש** ליעד הבא במקום להיעלם ולהופיע, וזה גם מחליק את הדגימה כל 120ms בזמן
   שהעמוד גולל את היעד לתצוגה. תוכן הכרטיס ממופתח `key={i}` עם `animate-tour-step`
   (fade + 6px, 220ms). ⚠️ `animate-pulse` נשמר על הטבעת — הוא נוגע רק ב-opacity
   ולכן לא מתנגש עם המעבר הגיאומטרי, והוא מה שאומר "על זה להקיש".
4. **סיום מחזיר לבית**: `onDone` מכבה את מסך המדדים ומחזיר לטאב הבית. השלב האחרון
   עומד על מסך המדדים, וסגירה שם השאירה את המלצר בדף סטטיסטיקות במקום במשימות.
   חל גם על "דלגו על הסיור", שאפשר ללחוץ מכל שלב.

### 🍚 אורז ואצה יצאו מהאפשרויות (2026-08-24)

בקשת משתמש: **"תוריד מהאופציות בשאלות אורז ואצה — זה לא המרכיבים שישאלו עליהם."**
נכון פעמיים: הם נמצאים בכל רול בתפריט, ולכן (א) כשאלה הם לא מלמדים כלום — התשובה
היא "כולם"; (ב) כצ'יפ חובה הם **מענישים** מלצר שמכיר את המנה על שדילג על המובן מאליו.

`askableIngredients(item)` ב-`questionEngine.js` (רשימה: אורז · אורז סושי · אצה ·
נורי · אצות, מנורמל דרך `hebKey`). מוחל על **כל מקום שבו מרכיב הופך לאופציה**:
- `qNotIngredient` — גם ברשימת הנכונים וגם במאגר הפולשים מהמנות הדומות.
- `qCompose` (מבחן מלא) — גם הסט הנכון וגם המסיחים.
- `qWithIngredient` — "3 מנות עם X" לא ייבנה סביב אורז.
- `CategoryExam` — המאגר, ו**מפתח התשובה** (`realIng`). ⚠️ בלי לסנן גם את מפתח
  התשובה, המלצר היה מסומן כמפספס צ'יפ שכלל לא הופיע על המסך.

⚠️ **הנתונים לא נגעו** — הכרטיסייה ומסך המנה עדיין מציגים את המתכון המלא. הסינון
הוא על אפשרויות שאלה בלבד.
נבדק חי (בוחן מאקי, סטודיו 2026): האפשרויות הן טמפורה · בצל ירוק · סלמון · טונה
אדומה · טריאקי · בטטה · אבוקדו · שרימפס · ירק לבחירה — בלי אורז ובלי אצה.
בדיקת רגרסיה ב-`tests/scenarios.test.mjs` (60 הגרלות על תפריט רולים ⇒ אף פעם לא מוצע).

### 🎨 מקרא הצבעים עבר לכניסה לתפריט (2026-08-24)

בקשת משתמש: הכרטיסיות ממילא כותבות "אלרגיות: …" ו"מוקשים: …" **במילים**, אז מקרא
לפני התרגול הוא מסך שמסביר את מה שהמסך הבא אומר בעצמו. בתפריט, לעומת זאת, אותו
מידע הוא צ'יפים צבעוניים בלי כותרת — ושם צריך ללמוד את הקוד.
הגייט עבר מ-`mode` ל-`tab === "categories"`, והכותרת/הכפתור ל"רגע לפני התפריט"
ו"הבנתי — לתפריט".

## 🎬 2026-08-24 — סרטון פתיחה במקום הטוטוריאל (סלון יווני)

בקשת משתמש: **מלצר חדש בסלון יווני יקבל את הסרטון שהכנו במקום הטוטוריאל הידני.**

**פר-מסעדה, לא hardcoded** (עקרון הטפטים): עמודה חדשה
`menu_app.restaurants.welcome_video_url`, ו-`team_join` מחזיר אותה בתוך אובייקט
המסעדה (שינוי אדיטיבי בלבד — כל השאר בפונקציה לא נגע). מסעדה בלי ערך ⇒ הטוטוריאל
הרגיל, בלי שינוי. הוספת המסעדה הבאה = קובץ תוכן ב-`crewmenu-video` + `UPDATE` אחד.

- **הסרטון**: `crewmenu-video/out/crewmenu-tour.mp4` הוא **מהדורת SALON**
  (`EDITIONS` ב-`Root.tsx`: `CrewMenuPromo`=SALON, `CrewMenuStudio`=STUDIO).
  ⚠️ שלוש המהדורות באורך זהה (67.4 שניות) — **אי אפשר להבחין ביניהן לפי משך**;
  זיהיתי לפי פריים בשנייה 14 (שם מופיע "דנה כהן · סלון יווני").
- **קודד מחדש למובייל**: 1080×1920/13.9MB ⇒ 720×1280, CRF 28, `+faststart`,
  בלי אודיו ⇒ **2.1MB**. הוא נטען על רשת סלולרית של מלצר, לא בדסקטופ.
  יושב ב-`public/welcome-salon.mp4` ומוגש סטטית מ-Vercel.
- **`src/screens/WelcomeVideo.jsx`** (חדש) — תופס את אותה משבצת של `showTutorial`,
  כלומר שאר זרימת הכניסה לא השתנתה. ⚠️ `muted` + `playsInline` הם **התנאי היחיד**
  ש-iOS Safari מתניע בו וידאו בלי מגע; יש גם כפתור Play לדפדפן שמסרב, ו**"לדלג
  ולהתחיל" גלוי תמיד** — מלצר שפתח את זה באמצע משמרת לא ננעל ל-67 שניות.
  `onError` מסמן כאילו הסתיים: וידאו שלא נטען לא יכול לחסום כניסה.

⚠️ **מי הן שתי "סלון יווני" ב-DB**: `SALON`/team `1234` ("סלון יווני צומת סביון")
היא האמיתית — 19 מנות, 16 חברי צוות, פעילות מהשבוע. `5315`/team `B49D03`
("סלון יווני") היא רשומה ריקה (0 מנות, 0 חברים). הסרטון הוצמד לראשונה.

**שתי מסעדות, שתי מהדורות (2026-08-24, אותו יום)**: סטודיו קיבלה את מהדורתה
(`/welcome-studio.mp4`), וסלון קיבלה **רינדור מחדש** — הפרויקט השתנה מאז הרינדור
הראשון והתווסף פסקול (`public/tour-music.m4a`), כלומר הקובץ הקודם היה מיושן **ובלי
מוזיקה** (הקידוד הראשון גם הפשיט אודיו ב-`-an`).
⚠️ **המהדורות נבדלות רק בתוכן, לא באורך** — שתיהן 67.4 שניות. זיהוי לפי פריים
בשנייה 14: "דנה כהן · סלון יווני" מול "מאיה ברק · סטודיו תל אביב".
⚠️ **הקידוד שומר אודיו עכשיו** (AAC 96k) — 720p/CRF28/faststart, ~3MB לכל אחת.
⚠️ **הווידאו מתחיל מושתק** כי זה התנאי היחיד לאוטופליי בדפדפן — ולכן נוסף **כפתור
קול** בפינה. בלעדיו הפסקול היה קיים בקובץ ואף מלצר לא היה שומע אותו.

נבדק חי: `1234` ⇒ מהדורת סלון · `AA46D8` ⇒ מהדורת סטודיו · מסעדה בלי ערך ⇒ שקפי
הטוטוריאל כרגיל.
